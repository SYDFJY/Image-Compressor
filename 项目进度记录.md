# 项目进度记录与问题日志

> 实时记录开发进度、遇到的问题与解决方案、技术决策理由。

---

## 2026-07-11 · 项目重大升级：从 Image Compressor → NCHU Compressor

### 升级概述

原项目 `E:\claude\Project\Image Compressor` 为纯图片压缩工具，现整体迁移至 `E:\claude\Project\Compressor`，升级为**图片 + 视频双模压缩工具**。

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| 项目迁移 | ✅ 完成 | 源码/文档/构建脚本/分发目录完整复制 |
| Git 初始化 | ✅ 完成 | `0b6837f init: 项目初始化`，57 文件 11703 行 |
| 项目文档更新 | ✅ 完成 | 5 份核心文档全部更新为新方向（CLAUDE.md / 进度记录 / 实现计划 / 总结报告 / 测试清单） |
| 编译修复 | ⏳ 待解决 | CLASSPATH 环境变量干扰 javac（见下方问题记录） |

### 项目新定位

| 维度 | 旧 | 新 |
|------|-----|-----|
| 项目名 | NCHU Image Compressor | **NCHU Compressor** |
| 功能范围 | 仅图片压缩 | **图片压缩 + 视频压缩** |
| 图片引擎 | Thumbnailator | Thumbnailator（不变） |
| 视频引擎 | — | **FFmpeg（外部 CLI）** |
| 分发方式 | JRE 捆绑免安装版 | JRE + FFmpeg 捆绑免安装版（待实现） |

### 迁移内容

```
E:\claude\Project\Compressor/
├── src/main/java/com/nchu/imagecompress/  (35 个 Java 源文件，MVC 架构)
├── src/main/native/launcher.c              (Windows C 启动器)
├── dist/                                    (EXE + JAR + VBS 启动器)
├── 项目计划书.pdf                           (684 KB)
├── 项目总结报告.md / 项目总结.pdf
├── 项目进度记录.md / 项目实现计划.md
├── 测试用例清单.md / CLAUDE.md
├── pom.xml / build-exe.bat / .gitignore
└── mvnw / mvnw.cmd (Maven Wrapper)
```

### 已知问题

| # | 问题 | 影响 | 状态 |
|---|------|------|------|
| 1 | ~~CLASSPATH 环境变量干扰 javac~~ | ~~`mvn clean compile` 失败~~ | ✅ 已解决（见下方） |
| 2 | 新项目 Git remote 未配置 | 无法 push 到 GitHub | ✅ 已解决（force push） |

### 视频压缩核心框架 · 开发完成 ✅

**提交**: `491ec74 feat(model,util,service,view,controller): 实现视频压缩核心框架`

**涉及文件**（16 个）:
- 新增 9 个：FileInfo / VideoFileInfo / VideoCompressConfig / VideoUtil / VideoCompressUtil / VideoCompressService / VideoParamPanel / VideoPreviewPanel
- 修改 7 个：ImageFileInfo / AppConfig / CompressResult / MainControllerCallback / MainController / MainFrame / ResultDialog

**代码量**: +2780 行, -60 行

**关键设计决策**:
1. **FileInfo 公共接口**：ImageFileInfo 和 VideoFileInfo 统一实现 FileInfo，便于 FileListPanel 未来支持混合文件列表
2. **模式隔离**：图片/视频模式通过 MainFrame CardLayout 切换右侧面板，MainController 中 if/else 分发，互不干扰
3. **FFmpeg 进度解析**：通过正则解析 stderr 中 `time=HH:MM:SS.ms` 字段计算进度百分比
4. **Gson 复用**：使用已有 Gson 依赖解析 ffprobe JSON，无需新增 Jackson 依赖

**视频参数配置**:
| 参数 | 默认值 | 范围 |
|------|--------|------|
| CRF | 23 | 0-51（0=无损, 51=最低质量）|
| 分辨率 | 原始 | 原始/480p/720p/1080p/4K |
| 帧率 | 保持原始 | 原始/24/30/60 fps |
| 音频 | 保留 | 保留/移除 |
| 输出格式 | 原格式 | 原格式/MP4(H.264)/WebM(VP9)/AVI/MOV/MKV |

**3 套预设**:
- 微信小视频：CRF 30 + 480p
- B站投稿：CRF 20 + 1080p + 30fps
- 存档原画：CRF 15 + 原始分辨率

**编译状态**: ⚠️ 视频代码无编译错误，仅 FileListPanel 的 Thumbnailator 导入受 CLASSPATH env var 影响（已知问题 #1）

### CLASSPATH 编译错误修复 ✅

**提交**: `d937421 fix(project): 修复编译错误 — Thumbnailator 导入拼写错误 + JDK 8 lambda 变量作用域`

**根因**: `FileListPanel.java:32` 的 import 拼写错误——`net.coobird.thumbnails.Thumbnails`（缺 "a" 和 "tor"），正确应为 `net.coobird.thumbnailator.Thumbnails`。这不是 CLASSPATH 环境变量问题。

**额外修复**: `MainController.java` 中 JDK 8 lambda 表达式引用非 effectively-final 变量 `i`/`result`/`info`，加 `final` 局部变量声明。

---

### FileListPanel FileInfo 接口改造 ✅

**提交**: `9258294 refactor(view,controller): FileListPanel 改造为 FileInfo 统一接口 — 支持图片/视频双模文件列表显示`

**涉及文件**（2 个）:
- `view/FileListPanel.java`: +178/-89 — 全部类型从 `ImageFileInfo` 改为 `FileInfo` 接口
- `controller/MainController.java`: +33/-14 — 适配 FileInfo 接口，视频模式同步到 fileListPanel

**关键改动**:
1. `DefaultListModel<ImageFileInfo>` → `DefaultListModel<FileInfo>`，`JList<ImageFileInfo>` → `JList<FileInfo>`
2. 渲染器通过 `instanceof VideoFileInfo` / `instanceof ImageFileInfo` 区分显示（🎬 图标+时长 vs 🖼 图标+尺寸）
3. 新增 `getImageFileList()` / `getVideoFileList()` 辅助方法，渐进式迁移旧代码
4. `loadThumbnail()` 跳过视频文件（视频无缩略图）
5. 底部统计栏区分图片/视频数量
6. MainController 模式切换时同步 `fileListPanel` ↔ `videoFileList`
7. 视频导入后同时填充 `videoFileList` + `fileListPanel.setFileList()`

**设计决策**:
- 保留 `videoFileList` 作为视频数据的 source of truth，`fileListPanel` 作为视图投影
- 模式切换时（图片↔视频），`fileListPanel.setFileList(videoFileList)` 重新投影，不丢失数据
- 旧代码通过 `getImageFileList()` 获取类型安全的 `List<ImageFileInfo>`，无需全局 instanceof 转换

### 下一步

1. ~~修复 CLASSPATH 编译问题~~ ✅
2. ~~配置 Git remote 并推送~~ ✅
3. ~~FileListPanel 改造~~ ✅
4. ~~分发打包更新~~ ✅
5. 集成测试：在安装 FFmpeg 的环境中实测视频压缩全流程
6. 免安装版 FFmpeg 捆绑（当前 standalone 仅有 JRE，需捆绑 FFmpeg 二进制）

---

### 分发打包更新 ✅

**提交**: `dca088d chore(project): 更新分发打包 — fat JAR 重建 + README 双模文档 + 启动脚本升级`

---

### 视频压缩单元测试 ✅

**提交**: （待提交）

**新增测试文件**（3 个）:
- `VideoCompressUtilTest.java`: 23 tests — parseTime / buildFfmpegCommand / generateOutputFile
- `VideoCompressConfigTest.java`: 21 tests — 默认值 / CRF 边界 / 预设 / fromExtension / isEffectivelyNoOp
- `VideoFileInfoTest.java`: 25 tests — 构造函数 / 格式化大小 / 时长显示 / FileInfo 接口 / equals

**测试结果**: 全部 69 个测试通过，0 失败 0 错误

**覆盖范围**:
| 被测类 | 测试数 | 覆盖功能 |
|--------|--------|---------|
| VideoCompressUtil | 23 | parseTime (9), buildFfmpegCommand (8), generateOutputFile (6) |
| VideoCompressConfig | 21 | 默认值 (1), CRF 边界 (4), 预设 (3), fromExtension (6), isEffectivelyNoOp (5), 枚举值 (2) |
| VideoFileInfo | 25 | 构造 (2), 时长格式化 (8), 大小格式化 (5), FileInfo 接口 (4), equals/hashCode (5) |

**设计要点**:
- 所有测试不依赖 FFmpeg 安装，纯 Java 逻辑验证
- 使用 JUnit 4 (已有依赖)，`mvn test` 自动运行
- `buildFfmpegCommand` 测试验证 CLI 参数结构正确但不执行命令

### 下一步

1. ~~修复 CLASSPATH 编译问题~~ ✅
2. ~~配置 Git remote 并推送~~ ✅
3. ~~FileListPanel 改造~~ ✅
4. ~~分发打包更新~~ ✅
5. ~~视频压缩单元测试~~ ✅
6. 集成测试：在安装 FFmpeg 的环境中实测视频压缩全流程
7. 免安装版 FFmpeg 捆绑（FFmpeg Windows 便携版 ~40MB）

---

## 2026-07-08 · 第 1 天（项目骨架 + Model 层）

## 2026-07-08 · 第 1 天（项目骨架 + Model 层）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| Maven 环境准备 | ✅ 完成 | 下载 Apache Maven 3.9.6 到本地 tools 目录 |
| pom.xml 配置 | ✅ 完成 | FlatLaf 3.2.5 / Thumbnailator 0.4.20 / Gson 2.8.9 / JUnit 4.13.2 |
| Maven Wrapper 生成 | ✅ 完成 | 项目根目录含 mvnw / mvnw.cmd |
| 包结构创建 | ✅ 完成 | 5 个包目录 + test + resources |
| App.java 入口 | ✅ 完成 | EDT 中启动 Swing |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS |
| Model 层 | ✅ 完成 | 2 枚举 + 5 实体类，共 7 个文件 |

---

**Model 层清单：**
| 文件 | 类型 | 说明 |
|------|------|------|
| OutputFormat.java | 枚举 | 输出格式定义（ORIGINAL/JPEG/PNG/BMP/WEBP） |
| Theme.java | 枚举 | 主题定义（LIGHT/DARK/INTELLIJ/DARCULA） |
| ImageFileInfo.java | 实体 | 文件信息（含 Status 内部枚举、文件大小格式化） |
| CompressConfig.java | 实体 | 压缩参数（含 ScaleMode/NamingRule 内部枚举、参数边界校验） |
| CompressResult.java | 实体 | 压缩结果（含失败工厂方法、压缩率/耗时格式化） |
| ProgressChunk.java | 实体 | 进度数据块（3 个工厂方法对应不同进度场景） |
| AppConfig.java | 实体 | 全局配置（窗口状态、上次参数、最近目录，可 JSON 序列化） |

### 问题与解决

| # | 问题 | 原因 | 解决方案 | 耗时 |
|---|------|------|---------|------|
| 1 | `mvn` 命令未找到 | 系统未预装 Maven | 下载 apache-maven-3.9.6-bin.zip 到 `~/tools/`，手动配置 PATH | ~3min |
| 2 | Apache Archive 下载超时 | archive.apache.org 国内访问慢 | 改用 Maven Central (repo1.maven.org) 下载 | ~1min |

### 技术决策

1. **Maven Wrapper 为什么用 3.9.6？** — 3.9.x 最后支持 JDK 8 的版本线，Maven 4.0+ 需要 JDK 17+
2. **pom.xml 中用 `${property}` 管理版本号** — 统一版本控制，后续升级只需改一处
3. **assembly plugin 配置 `appendAssemblyId=false`** — 让 fat JAR 输出为 `image-compressor-1.0.0.jar` 而非 `image-compressor-1.0.0-jar-with-dependencies.jar`

---

## 2026-07-08 · 第 2 天（Util 工具层 + ConfigService）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| FileUtil.java | ✅ 完成 | 格式校验 / 扩展名 / 大小格式化 / 唯一文件名 / 目录操作 |
| ImageUtil.java | ✅ 完成 | 尺寸读取 / 缩略图 / 预览图 / 效果预览 / ImageIcon 创建 |
| ValidateUtil.java | ✅ 完成 | 质量/缩放/路径/文件综合校验，含 ValidationResult 内部类 |
| ConfigService.java | ✅ 完成 | JSON 读写 + 500ms Timer 防抖 + 损坏备份 + shutdown 钩子 |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，12 个源文件 0 错误 0 警告 |

### 技术决策

1. **FileUtil 的防抖生成唯一文件名** — 支持最多 9999 个递增编号，超出后改用时间戳避免死循环
2. **ImageUtil 缩略图/预览图分离** — 缩略图 (`generateThumbnail`) 用于列表，固定 200×150；预览图 (`generatePreview`) 用于对比面板，最大 1024×768；不同场景不同尺寸避免内存浪费
3. **ValidateUtil 返回 ValidationResult 而非 boolean** — 携带错误消息字符串，方便 UI 层直接显示给用户，无需额外翻译步骤
4. **ConfigService 防抖机制** — 滑块拖拽可能每秒触发几十次 saveConfig 调用，用 500ms Timer 防抖合并为一次磁盘写入；`shutdown()` 强制刷盘保证退出时不丢数据
5. **损坏配置文件自动备份** — 读取到损坏 JSON 时自动重命名为 `.bak`，然后写入新的默认配置，防止用户手动修改出错后数据永久丢失

---

## 2026-07-08 · 第 3-4 天（Service 业务层核心逻辑）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| ProgressListener.java | ✅ 完成 | 5 个回调方法，完整时序文档 |
| ImageCompressUtil.java | ✅ 完成 | 压缩核心引擎：质量/缩放/格式转换/PNG白底填充 |
| CompressService.java | ✅ 完成 | 单张压缩 + 三规则文件命名 + 自冲突检测 |
| BatchCompressService.java | ✅ 完成 | 批量遍历 + 中断取消 + 单文件容错 |
| FileManagerService.java | ✅ 完成 | 多源导入/格式过滤/去重/递归扫描 |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，17 个源文件 0 错误 0 警告 |

### 关键设计

1. **PNG→JPG 白色背景填充** — 不使用 Thumbnailator 的默认行为（透明→黑色），而是手动创建 `TYPE_INT_RGB` BufferedImage + `Color.WHITE` 填充 + `drawImage`
2. **取消机制：Thread.interrupt()** — Service 层不引入 SwingWorker 依赖，使用标准 JDK 中断机制；Controller 层调 SwingWorker.cancel(true) 即可设置中断标志
3. **FileManagerService 单次导入上限 500 文件** — 防止拖入大量文件时界面卡顿 + OOM
4. **CompressResult 使用工厂方法 fail()** — 区分"压缩失败"和"校验不通过"，前者是 Thumbnailator 异常，后者是参数/文件错误

---

## 2026-07-08 · 第 5-6 天（UI 骨架 — 骨架先行策略）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| ThemeUtil.java | ✅ 完成 | FlatLaf 主题加载/切换/全局扁圆角+字体+强调色 |
| SplashScreen.java | ✅ 完成 | JWindow 启动画面 (420×280, 蓝色品牌色) |
| MainFrame.java | ✅ 完成 | JFrame 四栏布局 + 完整 JMenuBar + JToolBar + 双 JSplitPane |
| FileListPanel.java | ✅ 完成 | JList + 自定义 ListCellRenderer(文件名/大小/状态图标) + 统计栏 |
| PreviewPanel.java | ✅ 完成 | 原图/效果图切换 + 底部数据对比栏(原始/压缩后/压缩率) |
| ParamPanel.java | ✅ 完成 | JTabbedPane(基础/高级) + JSlider 质量 + 格式/缩放/命名选择 |
| StatusBar.java | ✅ 完成 | JLabel 状态文字 + JProgressBar 进度条 |
| App.java 更新 | ✅ 完成 | 启动流程: 主题→SplashScreen→MainFrame→dispose |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，24 个源文件 0 错误 0 警告 |

### 关键设计

1. **MainFrame 双 JSplitPane 嵌套** — 水平分左 36% 右 64%，右侧再垂直分上 60% 下 40%，用 `setResizeWeight()` 保证拉伸时的比例分配
2. **MenuBar 完整快捷键** — Ctrl+O 导入、Ctrl+Shift+O 导入文件夹、Ctrl+Delete 清空、Ctrl+T 切换主题、Ctrl+Q 退出、F1 关于
3. **FileListPanel 自定义 ListCellRenderer** — 继承 JPanel 绘制多行信息(文件名/尺寸/状态)，奇偶行不同背景色，选中/未选中配色
4. **ParamPanel 用 JTabbedPane 组织** — 基础设置(质量/缩放/格式)在第一个 Tab，高级设置(命名/目录/覆盖)在第二个，避免面板过长

---

## 2026-07-08 · 第 7 天（界面视觉精修 — 动效/Toast/渲染细化）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| AnimationUtil.java | ✅ 完成 | 4 种缓动函数 + 淡入/淡出/滑入/滑出/脉冲动画，全基于 javax.swing.Timer |
| ToastNotification.java | ✅ 完成 | JWindow 右下角弹出，4 类型(成功/错误/信息/警告)，自动队列 + 滑入动画 + 3s 自动消失 |
| ThemeUtil.java 更新 | ✅ 完成 | 新增：ScrollBar 细滚动条(8px)、JSlider 轨道色/滑块色、Button 悬停背景、TabbedPane 选中态 |
| FileListPanel 渲染器精修 | ✅ 完成 | 格式图标(🖼/🎞/📷)、彩色状态圆点 + 文字、选中强调色条、行分隔线 |
| StatusBar 精修 | ✅ 完成 | 状态图标(●/◉/★/✖) + 文字联动、flashSuccess/flashError 闪烁方法 |
| PreviewPanel 双画布 | ✅ 完成 | CardLayout 管理三张卡片(空状态/原图/效果图)，切换按钮组带选中高亮 |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，26 个源文件 0 错误 0 警告 |

### 关键设计

1. **AnimationUtil 三次方缓入缓出** — 用 `easeInOut(t) = 4t³` (前半段) 和 `0.5×(2t-2)³+1` (后半段) 模拟物理加速/减速，避免线性动画的生硬感
2. **Toast 全局队列机制** — 静态 `Queue<ToastItem>` + `isShowing` 标志，多条 Toast 自动排队依次显示，避免屏幕右下角堆叠
3. **FileListCellRenderer 左侧强调色条** — 3px 宽 JPanel，选中时显示 `#409EFF` 蓝色，类似 VS Code 的文件列表选中效果
4. **PreviewPanel CardLayout 三卡片** — "空状态"、"原图"、"效果图"三张卡片相互独立，不再共用 JLabel，避免图标残留问题
5. **StatusBar 状态类型方法** — `setStatus(text, type)` 一次性设置图标+文字+颜色，支持 ready/working/success/error 四种预设

---

## 2026-07-08 · 第 8-9 天（Controller 层 + 功能联调）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| MainControllerCallback.java | ✅ 完成 | View→Controller 回调接口，14 个方法覆盖所有用户操作 |
| CompressWorker.java | ✅ 完成 | SwingWorker 封装，publish/process 进度 + done 完成回调 |
| MainController.java | ✅ 完成 | 核心调度中心：导入/压缩/预览/配置/主题/窗口全流程 |
| App.java 更新 | ✅ 完成 | 集成 MainController，启动流程完整闭环 |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，29 个源文件 0 错误 0 警告 |

### 关键设计

1. **MainController 依赖注入** — 构造函数注入 MainFrame → 从中获取所有面板引用 → 初始化所有 Service 实例，遵循"谁持有谁负责"原则
2. **文件导入后台线程** — `importFilesInternal()` 将 FileManagerService 调用放入新线程（因为需要读取每张图片的尺寸），完成后通过 `SwingUtilities.invokeLater()` 回到 EDT 更新 UI
3. **压缩流程 EDT 安全** — 构建 CompressConfig(EDT) → CompressWorker.execute()(后台) → process()(EDT 更新进度条+列表) → done()(EDT 显示结果Toast)
4. **配置防抖保存** — 滑块拖动时：ChangeListener → onQualityChanged() → configService.saveConfig()(500ms防抖)；窗口关闭时：saveConfigImmediately()(立即写入)
5. **取消机制双层保护** — 用户点击取消 → CompressWorker.cancel(true) → 中断后台线程 → BatchCompressService 检测 Thread.interrupted() → 停止循环 → onCompressCancelled() 恢复 UI
6. **拖拽导入 TransferHandler** — 在 MainFrame、FileListPanel、JList 三级组件上都设置 TransferHandler，确保拖拽到任何区域都能触发导入

### 完整事件绑定清单

| 事件源 | 事件 | 处理方法 |
|--------|------|---------|
| 菜单：导入图片 (Ctrl+O) | Action | onImportFiles() → JFileChooser → FileManagerService |
| 菜单：导入文件夹 (Ctrl+Shift+O) | Action | onImportFolder() → JFileChooser(DIRECTORIES) |
| 菜单：清空列表 (Ctrl+Delete) | Action | onClearAllFiles() |
| 菜单：设置 (Ctrl+,) | Action | onOpenSettings() → JOptionPane |
| 菜单：切换主题 (Ctrl+T) | Action | onToggleTheme() → ThemeUtil.switchTheme() |
| 菜单：关于 (F1) | Action | onOpenAbout() → JOptionPane |
| 菜单：退出 (Ctrl+Q) | Action | onExit() → onWindowClosing() |
| 工具栏：导入/导入文件夹/清空/压缩/主题 | Action | 同菜单对应方法 |
| ParamPanel：开始压缩 | Action | onStartCompress() → CompressWorker |
| ParamPanel：取消 | Action | onCancelCompress() → worker.cancel(true) |
| ParamPanel：选择输出目录 | Action | onChooseOutputDir() → JFileChooser |
| ParamPanel：质量滑块 | ChangeListener | onQualityChanged() → configService.saveConfig() |
| FileListPanel：列表选中 | ListSelectionListener | onFileSelected() → ImageUtil.loadScaledIcon() |
| FileListPanel：清空按钮 | Action | onClearAllFiles() |
| 窗口：关闭 | WindowListener | onWindowClosing() → saveWindowState() → shutdown() |
| 窗口：移动/调整大小 | ComponentListener | saveWindowState() |
| 全局：拖拽文件 | TransferHandler | onDragImport() → importFilesInternal() |
| JList：右键菜单 | MouseListener | 预览/打开原图/打开文件夹/单张压缩/移除/清空 |
| JList：Delete 键 | KeyListener | onRemoveFile(selectedIndex) |

---

## 2026-07-08 · 第 10-11 天（批量处理调优 + 高级功能）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| ResultDialog.java | ✅ 完成 | JDialog 结果弹窗：统计卡片 + 详情行 + 打开输出目录按钮 |
| SmartRecommendService.java | ✅ 完成 | 图片类型识别（图标/截图/照片/未知）+ 个性化质量/格式推荐 |
| FileListPanel 右键菜单 | ✅ 完成 | 6 项菜单：预览/打开原图/打开文件夹/单张压缩/移除/清空 |
| Delete 键移除 | ✅ 完成 | 选中文件后按 Delete 键从列表移除 |
| MainController 增强 | ✅ 完成 | 集成 ResultDialog + SmartRecommend + 右键菜单绑定 + 单文件压缩 |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，31 个源文件 0 错误 0 警告 |

### 关键设计

1. **ResultDialog 独立统计** — 弹窗内部自行遍历 CompressResult 列表统计，不依赖 Controller 传入预计算值，减少 Controller 与 View 之间的数据耦合
2. **SmartRecommendService 启发式分类** — 基于尺寸阈值（300px 视作图标）+ 宽高比匹配（16:9/4:3/3:2 误差 ±5%）+ 文件大小辅助判定（>100KB 更可能是照片），无需读取像素内容，性能开销极低
3. **单文件压缩复用 CompressService** — 右键"单独压缩"直接调用 `compressService.compress()`（非批量），完成后自动更新预览面板并显示对比数据
4. **右键菜单智能选中** — `MouseListener.locationToIndex()` + `setSelectedIndex()` 确保右键位置自动选中对应行，无需先左键再右键
5. **自动预览第一个成功文件** — 批量压缩完成后，遍历结果找到第一个成功的输出文件并在预览面板显示效果图

### 新增功能一览

| 功能 | 触发方式 | 说明 |
|------|---------|------|
| 结果弹窗 | 批量压缩完成自动弹出 | 成功/失败/节省空间统计 + 打开输出目录 |
| 右键预览 | 右键菜单 → 预览 | 选中并加载预览图 |
| 右键打开原图 | 右键菜单 → 打开原图 | 系统默认程序打开原文件 |
| 右键打开文件夹 | 右键菜单 → 打开所在文件夹 | 打开文件所在目录 |
| 单文件压缩 | 右键菜单 → 单独压缩此文件 | 只压缩选中文件 + 自动预览效果 |
| Delete 移除 | 选中后按 Delete 键 | 从列表移除选中文件 |

---

## 2026-07-08 · 第 12 天（细节优化 + Bug 修复）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| LogUtil.java | ✅ 完成 | 统一日志工具，封装 java.util.logging.Logger，支持 INFO/WARNING/SEVERE 三级 |
| System.out/err 替换 | ✅ 完成 | 7 个文件 / 25 处全部替换为 LogUtil 调用 |
| isEffectivelyNoOp 接入 | ✅ 完成 | 压缩前检测无效参数组合并弹窗确认 |
| 边缘场景审查 | ✅ 完成 | 确认质量钳制/OOM保护/0字节检测/权限检查/取消机制全部就位 |
| MainFrame TODO 清理 | ✅ 完成 | 移除过期的图标占位注释 |
| 编译验证 | ✅ 通过 | `mvn clean compile` BUILD SUCCESS，32 源文件 0 错误 0 警告 |

### 关键设计

1. **LogUtil 统一日志封装** — 全局单一 Logger 实例（名称 `NCHU.IC`），自定义 ConsoleHandler + 简洁格式 `[LEVEL] message`，禁用父 Handler 避免重复输出。比每个类各自创建 Logger 更轻量，适配学生项目场景。
2. **日志级别映射** — `System.out.println` → `LogUtil.info()`（正常流程），`System.err.println` → `LogUtil.warning()`（可恢复异常如配置损坏）或 `LogUtil.error()`（严重错误如写入失败）。遵循"警告不影响运行，错误需关注"的语义。
3. **isEffectivelyNoOp() 前移校验** — 原方法定义在 CompressConfig 中但从未调用。现接入 Controller 层，在压缩启动前检测（质量=100 + 不缩放 + 原格式=无效压缩），弹出确认框避免用户误操作后困惑"为什么文件没变小"。

### 边缘场景审查结果

| 场景 | 处理位置 | 机制 |
|------|---------|------|
| 空列表压缩 | MainController.onStartCompress() | 提前 return + Toast 提示 |
| 质量=0/100 | CompressConfig.setQuality() | `Math.max(0, Math.min(100, q))` 钳制 |
| 无效压缩参数 | MainController.onStartCompress() | isEffectivelyNoOp() → JOptionPane 确认 |
| 超大图片(>100M像素) | ImageCompressUtil.compress() | MAX_PIXELS 检查，提前拦截 |
| 超大文件(>500MB) | ValidateUtil.validateInputFile() | 文件大小上限检查 |
| 0字节文件 | ValidateUtil.validateInputFile() | length==0 检测 |
| 损坏图片 | ImageCompressUtil.compress() | IOException → fail result |
| 内存溢出 | ImageCompressUtil.compress() | OutOfMemoryError → fail result |
| 无写入权限 | ValidateUtil.validateOutputPath() | canWrite() 检查 |
| 中途取消 | BatchCompressService.compressBatch() | Thread.interrupted() 每次循环前检查 |
| 输出=输入覆盖 | CompressService.compress() | 同路径检测 → fail result |

---

## 2026-07-08 · 第 13 天（测试 + 打包）

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| Maven Assembly 打包 | ✅ 完成 | fat JAR 1.9 MB，含全部依赖，Main-Class 正确 |
| JAR 验证 | ✅ 完成 | MANIFEST.MF 校验通过，75 类 + 所有第三方库 |
| Windows 启动器 | ✅ 完成 | 启动.bat（有控制台）+ 启动.vbs（静默，推荐） |
| 用户指南 | ✅ 完成 | dist/README.md，含环境要求/启动方式/快捷键/FAQ |
| 测试用例清单 | ✅ 完成 | 72 项测试（功能30/交互15/配置6/异常12/兼容9） |
| 代码级验证 | ✅ 完成 | 72 项中 71 项通过逻辑审查，1 项需图形环境 |
| jlink 精简 JRE | ⏭ 跳过 | 当前环境仅 JDK 8（jlink 需 JDK 9+），用脚本替代 |
| Launch4j exe | ⏭ 跳过 | SourceForge 不可达（CRYPT_E_REVOCATION_OFFLINE），用 VBS 替代 |

### 关键设计

1. **VBS 静默启动器** — `CreateObject("WScript.Shell").Run "javaw ...", 0, False`，参数 0 隐藏窗口，javaw 无控制台，效果等同 exe 双击体验。比 bat 更优雅，不会闪现 CMD 黑框。
2. **bat 启动脚本保底** — 先 `chcp 65001` 支持 UTF-8 输出，`where java` 检测 JRE 环境，失败时给出友好提示和下载链接。`javaw`（而非 `java`）不阻塞控制台。
3. **交付目录结构** — `dist/` 包含：fat JAR + 两个启动器 + README，用户无需 Maven/Git/IDE，解压即用。符合"绿色免安装"交付标准。
4. **测试分级策略** — 功能测试 30 项覆盖核心流程，异常测试 12 项覆盖所有已知边界，兼容性测试 9 项覆盖格式/编码。M02（JDK 运行）标记为需图形环境手动验证。

### 交付目录结构

```
dist/
├── image-compressor-1.0.0.jar   ← fat JAR (1.9 MB, 可直接运行)
├── 启动.vbs                      ← Windows 静默启动器 (推荐双击)
├── 启动.bat                      ← Windows 批处理启动器 (有日志)
└── README.md                    ← 用户使用指南
```

---

## 2026-07-08 · UI 全面升级（莫兰迪色系 · 卡片化 · 精致控件）

### 设计规范

基于用户提供的「高级感 UI 升级规范」严格执行，定位现代极简轻商务风。

### 进度概览

| 模块 | 优先级 | 状态 | 改动量 |
|------|--------|------|--------|
| ThemeUtil 设计令牌 | P0 | ✅ 完成 | 完全重写：莫兰迪色系 + 8px 栅格 + 3 级字体 + 3 级圆角 + FlatLaf 全局属性 |
| MainFrame 工具栏 | P0 | ✅ 完成 | 一体化悬浮导航栏：左品牌/中操作/右功能 + 三区卡片化布局 |
| FileListPanel 列表 | P1 | ✅ 完成 | 缩略图 + 新渲染器(选中强调条/hover/失败态) + 空状态插画 |
| PreviewPanel 预览 | P1 | ✅ 完成 | 分段控件 + 棋盘格背景 + 半透明数据浮层(4 项均布) |
| ParamPanel 参数 | P1 | ✅ 完成 | 标签右对齐/控件左对齐 + 快捷预设按钮 + 主按钮 42px |
| StatusBar 状态栏 | P1 | ✅ 完成 | 32px 极简设计，无边框，进度条嵌入中间 |
| ToastNotification | P2 | ✅ 完成 | 配色同步至莫兰迪色系 |
| 编译验证 | — | ✅ 通过 | `mvn clean compile` BUILD SUCCESS，32 源文件 0 错误 0 警告 |

### 升级前后对比

| 维度 | 升级前 | 升级后 |
|------|--------|--------|
| 配色 | Element UI 风格 (#409EFF 亮蓝) | 莫兰迪色系 (#3B82F6 低饱和蓝) |
| 布局 | 传统 BorderLayout + 原生面板 | 白色卡片 + 圆角 + 窗口浅灰背景 |
| 工具栏 | JToolBar 原生样式 | 悬浮品牌导航栏（左品牌/中操作/右功能） |
| 文件列表 | 交替行色 + 原生选中态 | 缩略图 + 2px 强调条 + hover/失败差异化 |
| 预览区 | 普通 JLabel + 简陋切换按钮 | 分段控件 + 棋盘格背景 + 玻璃态数据浮层 |
| 参数面板 | 控件散落，对齐不一致 | 标签右对齐 + 控件左对齐 + 预设按钮 |
| 按钮层级 | 所有按钮样式一致 | 主按钮(42px 主色) / 次要(白底灰字) / 文字(仅hover) |
| 状态栏 | 普通标签 + 原生进度条 | 32px 极简，进度条嵌入中间 |

### 关键技术实现

1. **ThemeUtil 公共颜色常量** — 所有 UI 组件通过 `ThemeUtil.PRIMARY / TEXT_SECONDARY / BG_CARD` 等静态常量引用颜色，实现「改一处，全局同步」。
2. **卡片包裹器 `wrapCard()`** — MainFrame 中将每个功能面板包装为白色圆角卡片：`MatteBorder` 模拟轻阴影 + `LineBorder(BORDER, rounded)` 统一边框。
3. **分段控件** — PreviewPanel 中用 `JPanel.paintComponent()` 绘制圆角背景 + 选中项白色卡片叠加，避开了 Swing 无原生 SegmentedControl 的限制。
4. **棋盘格 CheckerboardPanel** — 继承 JPanel 覆写 `paintComponent()`，先绘制 16px 交错格纹，再调用 `super.paintComponent()` 渲染子组件，实现透明图片的显示标识。
5. **半透明数据浮层** — `paintComponent()` 中用 `setColor(new Color(255,255,255,220))` + `fillRoundRect()` 模拟毛玻璃效果。

### 涉及的源文件（9 个）

| 文件 | 改动 |
|------|------|
| ThemeUtil.java | 完全重写（公共颜色常量 + 全局 UIManager 属性） |
| MainFrame.java | 完全重写（悬浮工具栏 + 卡片包裹布局） |
| FileListPanel.java | 完全重写（新渲染器 + 空状态） |
| PreviewPanel.java | 完全重写（分段控件 + 棋盘格 + 数据浮层） |
| ParamPanel.java | 完全重写（强对齐表单 + 预设按钮） |
| StatusBar.java | 完全重写（32px 极简设计） |
| ToastNotification.java | 配色更新至莫兰迪色系 |
| dist/image-compressor-1.0.0.jar | 重新打包 |
| 项目进度记录.md | 本条目 |

---

## 2026-07-09 · Bug 修复轮次 1：格式转换系统性失败

### 问题发现

用户反馈：PNG 图片只能转为 JPEG，其他格式转换全部失败。经排查，根因有两个：

| # | Bug | 根因 | 影响范围 |
|---|-----|------|---------|
| 1 | 所有不触发 alphaFill 的转换失败 | Thumbnailator 0.4.20 `Builder` 要求至少调用一次 `scale()`/`size()`，`ScaleMode.NONE` 时未调用 | PNG/GIF→JPEG/BMP 以外全部失败 |
| 2 | 大图二次读取失败 | `ImageIO.read()` 全量解码后在 Windows 上可能未释放文件句柄，导致 Thumbnailator 再次 `of()` 时文件锁定 | 偶发性失败 |

### 修复内容

| 文件 | 改动 | 说明 |
|------|------|------|
| `ImageCompressUtil.java` | `applyScale()` NONE 分支加 `builder.scale(1.0)` | 满足 Thumbnailator 的强制 scale/size 调用要求 |
| `ImageCompressUtil.java` | `readDimensions()` 完全重写 | 用 `ImageReader.setInput()` 仅读 SOF 标记获取宽高（~2KB），替代 `ImageIO.read()` 全量解码（~72MB），同时避免文件锁定和 CMYK 解码失败 |
| `ImageUtil.java` | `readDimensions()` 同逻辑重写 | 与 ImageCompressUtil 保持一致 |
| `ImageCompressUtil.java` | 零尺寸检测 + 详细错误消息 | 当 `readDimensions()` 返回 {0,0} 时给出 CMYK/HEIC/WebP 等格式提示 |

### 验证

创建 `FormatConversionTest.java` 全面诊断测试，覆盖 6 种输入格式 × 4 种输出格式 = 24 种组合。修复后全部通过。

### Git 提交

```
fix(service,util): 修复格式转换系统性失败 — scale(1.0) + ImageReader 尺寸读取
```

---

## 2026-07-09 · Bug 修复轮次 2：移除 JDK 8 不支持的 WebP

### 问题发现

WebP 格式在 JDK 8 的 ImageIO 中既无 Reader 也无 Writer，但代码中仍保留了 WebP 输出格式选项和文件过滤器，导致用户选择后面临静默失败。

### 修复内容

| 文件 | 改动 |
|------|------|
| `OutputFormat.java` | 移除 `WEBP("WebP", "webp")` 枚举值 |
| `ParamPanel.java` | 格式下拉框移除 "WebP" 选项 |
| `MainController.java` | 移除 `case 4: config.setOutputFormat(OutputFormat.WEBP)` 和对应的配置恢复逻辑，更新文件过滤器 |
| `FileUtil.java` | `SUPPORTED_EXTENSIONS` 移除 `"webp"` |
| `ImageCompressUtil.java` | `needsAlphaBackgroundFill()` 移除 webp 检查 |
| `FormatConversionTest.java` | 移除 WebP 相关引用 |

### Git 提交

```
fix(model,view,controller): 移除 JDK 8 不支持的 WebP 输出格式
```

---

## 2026-07-09 · 测试：格式转换全面诊断

### 新增文件

`src/test/java/com/nchu/imagecompress/service/FormatConversionTest.java`（296 行）

- 自动创建 RGB + ARGB 测试图像，保存为 JPG/PNG/BMP/GIF 格式
- 遍历所有 24 种输入→输出组合
- 输出通过/失败矩阵 + 失败详情 + ImageIO 能力报告
- 零依赖，可直接 `java` 运行

### Git 提交

```
test(service): 添加格式转换全面诊断测试
```

---

## 2026-07-09 · Bug 修复轮次 3：启动画面卡死

### 问题发现

用户反馈：启动时出现蓝色弹窗（SplashScreen），无法关闭，只能强制结束进程。

### 根因分析

`App.java` 中原代码：

```java
splash.setVisible(true);
// ... 初始化 ...
Thread.sleep(300);   // ← EDT 上调用 sleep！
splash.dispose();
```

**致命问题**：`Thread.sleep(300)` 在 EDT（事件调度线程）上执行。Swing 是单线程模型，EDT 被阻塞时无法响应任何事件——包括 `dispose()` 调用和窗口关闭事件。如果 `sleep()` 前抛出任何异常，`dispose()` 永远不会执行，JWindow 永远挂在屏幕上。

### 修复方案（三层保护）

| 层 | 机制 | 作用 |
|----|------|------|
| 1 | `javax.swing.Timer` 替代 `Thread.sleep()` | Timer 在 EDT 上注册回调但不阻塞，300ms 后自动 `dispose()` |
| 2 | `try-finally` 包裹全部初始化 | 任何异常都会在 finally 中 `splash.dispose()` |
| 3 | 点击启动画面手动关闭 | `mouseClicked` → `dispose()`，用户可手动关闭 |

### 涉及文件

| 文件 | 改动 |
|------|------|
| `App.java` | Timer 替代 sleep + try-finally 保护 |
| `SplashScreen.java` | 新增 click-to-close 兜底监听器 |

### Git 提交

```
fix(view): 修复启动画面卡死 — Timer 替代 sleep + try-finally 保护 + 点击关闭兜底
```

---

## 2026-07-09 · GitHub 远程仓库推送

- 添加远程仓库：`https://github.com/SYDFJY/Image-Compressor.git`
- 推送 `master` 分支，共 8 个提交
- 所有提交均遵循 Conventional Commits 规范

---

## 问题日志（长期）

> 记录开发全程重大问题和对应解决方案，作为实训报告素材。

### #1 Thumbnailator 不调用 scale/size 导致压缩静默失败

**现象**：格式转换时抛出 `"size is not set"` 异常。

**原因**：Thumbnailator 0.4.20 的 `Builder` 在调用 `toFile()` 前必须至少调用一次 `scale()` 或 `size()`，即使不需要缩放也要显式 `scale(1.0)`。`ScaleMode.NONE` 原实现为空（no-op），不满足要求。

**解决**：`applyScale()` 的 NONE/default 分支改为 `builder.scale(1.0)`。

**教训**：第三方库的"默认行为"不可靠，关键路径必须有测试覆盖。

---

### #2 Windows 文件锁定导致 Thumbnailator 二次读取失败

**现象**：`ImageIO.read()` 读取尺寸后，`Thumbnails.of()` 再次打开同一文件时抛出 `IOException`。

**原因**：Windows 上 `ImageIO.read()` 全量解码大图后可能未释放文件句柄（取决于 ImageReader 实现），导致后续 `Thumbnails.of()` 无法打开文件。

**解决**：用 `ImageReader.setInput()` 仅读 SOF 元数据获取尺寸（~2KB），替代 `ImageIO.read()` 全量解码（~72MB）。同时解决了 CMYK JPEG 解码失败问题。

**教训**：Windows 文件锁比 Linux 严格得多，跨平台开发时必须注意文件句柄管理。

---

### #3 CMYK 色彩空间 JPEG 无法读取

**现象**：部分照片（尤其是单反相机直出）导入后尺寸显示为 0×0，压缩失败。

**原因**：JDK 8 的 JPEG ImageReader 不支持 CMYK 色彩空间，`ImageIO.read()` 返回 null。这些照片通常来自专业相机或 Photoshop 导出。

**解决**：改用 `ImageReader.setInput()` 读取 SOF 标记中的尺寸（标记段不涉及色彩空间解码），并在零尺寸时给出明确提示："CMYK 色彩空间 JPEG，建议用画图/Photoshop 另存为标准 RGB 格式"。

**教训**：JPEG 格式有多种色彩空间变体，不能假设所有 JPEG 都是 RGB。

---

### #4 JDK 8 不支持 WebP 格式

**现象**：输出格式中选择 WebP 后压缩失败。

**原因**：WebP 由 Google 在 2010 年发布，JDK 8（2014 年）的 ImageIO 不包含 WebP Reader/Writer。JDK 原生 WebP 支持需要 JDK 15+。

**解决**：从所有枚举、下拉框、文件过滤器中移除 WebP 选项。如需支持可通过 TwelveMonkeys 插件扩展，但超出实训范围。

**教训**：JDK 8 的 ImageIO 格式支持很有限（Reader: JPG/PNG/BMP/GIF/WBMP；Writer: JPG/PNG/BMP/GIF/WBMP），引入新格式前必须验证 JDK 8 兼容性。

---

### #5 EDT 阻塞导致启动画面卡死

**现象**：蓝色 SplashScreen 弹窗无法关闭。

**原因**：`Thread.sleep(300)` 在 EDT 上执行。Swing 是单线程事件循环，sleep 期间无法处理任何 UI 事件（包括窗口关闭）。

**解决**：三重保护——
1. `javax.swing.Timer` 替代 sleep（不阻塞 EDT）
2. `try-finally` 确保异常时也能 dispose
3. 点击画面手动关闭兜底

**教训**：**永远不要在 EDT 上 sleep/wait/执行耗时操作**。任何延迟需求都用 `javax.swing.Timer` 或 `SwingWorker`。

---

## 2026-07-09 · EXE 可执行文件打包（原生 Windows 启动器）

### 任务清单

| 任务 | 状态 | 备注 |
|------|------|------|
| 技术选型评估 | ✅ 完成 | Launch4j（网络不可达）→ javapackager（JRE 捆绑 190MB）→ 原生 C 启动器（33KB） |
| C 启动器开发 | ✅ 完成 | `src/main/native/launcher.c`，~130 行 |
| 启动器编译 | ✅ 完成 | gcc -mwindows -O2 -s，输出 33KB |
| 构建脚本 | ✅ 完成 | `build-exe.bat` |
| 发行目录更新 | ✅ 完成 | `dist/` 含 EXE + JAR + BAT + VBS |

### 关键设计决策

**为什么不用 Launch4j？**
Maven 插件 `com.akathist.maven.plugins:launch4j-maven-plugin:2.5.1` 在 Maven Central 上不存在，网络又不可达，无法下载。尝试失败后回退 pom.xml。

**为什么不用 javapackager 作为主方案？**
JDK 8 自带 javapackager 可创建独立 EXE（含捆绑 JRE），但体积达 190MB（剪裁后 114MB），不适合微信/QQ 分享。

**最终方案：原生 C 启动器**
- 用 C + Win32 API 写了一个 130 行的轻量启动器
- 自动在同目录查找 `image-compressor-*.jar`
- 按 3 层策略查找 Java：PATH → 注册表 → 常见安装路径
- 无 Java 时弹出中文错误提示对话框
- 编译后仅 **33KB**

### 涉及文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/main/native/launcher.c` | 新增 | 原生 Windows EXE 启动器源码 |
| `build-exe.bat` | 新增 | gcc 编译构建脚本 |
| `.gitignore` | 修改 | 放行 `dist/*.exe` |
| `dist/image-compressor.exe` | 新增 | 编译产物，33KB |
| `dist/NCHU-ImageCompressor-1.0.0.jar` | 删除 | 替换为最新 JAR |

### 分享方式

| 方式 | 文件 | 大小 | 对方需要 |
|------|------|------|---------|
| **轻量分享** | `image-compressor.exe` + `image-compressor-1.0.0.jar` | ~2MB | Java 8+ |
| 完整独立包 | `javapackager` 捆绑 JRE 包 | ~114MB | 无 |
| 源码分享 | `Image-Compressor-v1.0.0-source.zip` | 3.6MB | JDK 8 + Maven |

### 问题日志

### #6 Launch4j Maven 插件不可用

**现象**：`mvn package` 报 `com.akathist.maven.plugins:launch4j-maven-plugin:jar:2.5.1 was not found`

**原因**：该插件坐标在 Maven Central 上不存在，且当前网络不可达无法验证。Launch4j 有多个社区 Maven 插件，坐标各异且维护不稳定。

**解决**：放弃 Maven 插件路线，改用 JDK 8 自带的 javapackager + 自研 C 原生启动器。javapackager 生成完整独立包（114MB），C 启动器提供轻量方案（33KB）。

**教训**：依赖外部 Maven 插件前应先验证其在 Central 上的可用性。JDK 8 自带 javapackager 是更可靠的备选方案。原生 C 启动器虽需编译环境，但零依赖、极致轻量。

---

### #7 javapackager 原生 EXE 双击无反应

**现象**：`javapackager -native image` 生成的 `ImageCompressor.exe` 在命令行启动正常（日志输出"启动完成"），但 Windows 资源管理器中双击毫无反应，不弹窗口也不报错。

**原因**：javapackager 生成的 EXE 并非独立可执行文件 — 它是通过 `packager.dll` 直接加载 JVM 的瘦启动器（17KB）。该启动器依赖 `$APPDIR` 变量解析 runtime 路径，在部分 Windows 10/11 环境下双击时路径解析失败，导致 JVM 加载失败后静默退出。

**解决**：彻底放弃 javapackager 原生 EXE，改用 **捆绑 JRE + VBS 启动脚本** 方案：
- 从 JDK 8 复制完整 JRE（含 `java.exe`/`javaw.exe`）
- 安全剪裁：移除 JavaFX（51MB）、Nashorn JS 引擎（2MB）、部署工具（3MB），197MB → 117MB
- 用 VBS 脚本调用捆绑的 `jre\bin\javaw.exe -jar` 启动应用
- 最终 ZIP 压缩后 **47MB**，解压后双击 `启动.vbs` 即可运行

**涉及文件**：
| 文件 | 动作 | 说明 |
|------|------|------|
| `dist/standalone/启动.vbs` | 新增 | VBS 启动脚本，调用捆绑 JRE 的 javaw |
| `dist/standalone/jre/` | 新增 | 剪裁后的 JRE（117MB，已 gitignore） |
| `NCHU-Image-Compressor-免安装版.zip` | 更新 | 47MB，解压即用 |

**教训**：
1. javapackager 的 `-native image` 模式创建的 EXE 并不可靠，它在不同 Windows 版本上的 `$APPDIR` 解析行为不一致
2. 最简单的方案往往最可靠：JRE + 启动脚本的组合比任何原生 EXE 包装器都稳定
3. 测试必须在目标使用场景下进行 — 命令行 OK ≠ 双击 OK

### 最终交付物总结

| 文件 | 大小 | 对方需要 | 使用方式 |
|------|------|---------|---------|
| `dist/image-compressor.exe` + `dist/image-compressor-1.0.0.jar` | 2MB | Java 8+ | 双击 EXE |
| `NCHU-Image-Compressor-免安装版.zip` | 47MB | 无 | 解压后双击 `启动.vbs` |

---

## 2026-07-11 · 免安装版 FFmpeg 捆绑

### 任务清单

| 任务 | 状态 | 备注 |
|------|------|------|
| FFmpeg 二进制复制 | ✅ 完成 | ffmpeg.exe (138MB) + ffprobe.exe (138MB) → `standalone/ffmpeg/bin/` |
| standalone/启动.vbs 更新 | ✅ 完成 | 绝对路径 + `-Dffmpeg.bin.path` 指向捆绑版 FFmpeg |
| dist/启动.bat 更新 | ✅ 完成 | 优先检测 `standalone\ffmpeg\bin\ffmpeg.exe` |
| dist/启动.vbs 更新 | ✅ 完成 | 自动检测捆绑版 FFmpeg 并设置系统属性 |
| dist/README.md 更新 | ✅ 完成 | 新增免安装版说明 + FFmpeg 捆绑提示 |
| .gitignore 更新 | ✅ 完成 | 排除 `dist/standalone/ffmpeg/`（二进制过大，GitHub 不支持） |

### 捆绑方案

```
dist/standalone/
├── 启动.vbs                          ← 绝对路径 + -Dffmpeg.bin.path
├── image-compressor-1.0.0.jar        (2MB)
├── jre/                              (117MB, gitignored)
│   └── bin/javaw.exe
└── ffmpeg/                           (276MB, gitignored)
    └── bin/
        ├── ffmpeg.exe                (138MB)
        └── ffprobe.exe               (138MB)
```

### 关键设计决策

1. **仅捆绑 ffmpeg + ffprobe**：不包含 ffplay（播放器，与压缩无关），节省 138MB
2. **gitignore FFmpeg 二进制**：ffmpeg.exe + ffprobe.exe 共 276MB，远超 GitHub 100MB 单文件限制，且与 JRE 同为运行时依赖，仅在分发 ZIP 中包含
3. **启动脚本自动检测**：standalone VBS 使用 `-Dffmpeg.bin.path` 硬编码指向捆绑路径；dist 层脚本优先检测 `standalone\ffmpeg\bin\ffmpeg.exe` 是否存在，其次 fallback 到系统 PATH
4. **免安装版最终体积**：JRE (117MB) + FFmpeg (276MB) + JAR (2MB) ≈ **395MB** 解压后，ZIP 压缩后约 **160MB**

### 涉及文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `dist/standalone/ffmpeg/bin/ffmpeg.exe` | 新增 | FFmpeg 编码器（gitignored） |
| `dist/standalone/ffmpeg/bin/ffprobe.exe` | 新增 | 视频元数据解析器（gitignored） |
| `dist/standalone/启动.vbs` | 修改 | 绝对路径 + JRE/FFmpeg 检测 + `-Dffmpeg.bin.path` |
| `dist/启动.bat` | 修改 | 优先检测捆绑版 FFmpeg |
| `dist/启动.vbs` | 修改 | 自动检测捆绑版 FFmpeg |
| `dist/README.md` | 修改 | 新增免安装版章节 + FFmpeg 捆绑说明 |
| `.gitignore` | 修改 | 排除 `dist/standalone/ffmpeg/` |

### 下一步

1. 集成测试：在安装 FFmpeg 的环境中实测视频压缩全流程
2. 打包免安装版 ZIP：`dist/standalone/` → `NCHU-Compressor-免安装版.zip`
3. 混合文件列表：支持图片 + 视频在同一列表中（当前为模式分离）

---

## 2026-07-11 · 视频压缩对比持久化 + ffplay 视频播放

### 提交: `7c529a8 feat(view,controller,model,util): 视频压缩前后对比持久化 + ffplay 视频播放`

### Feature 1: 压缩对比持久化

**问题**：压缩完成后选中其他文件再切回来，对比数据消失 — `showVideoInfo()` 无条件隐藏了 comparisonPanel。

**方案**：将压缩结果写入 `VideoFileInfo` Model（`compressedSize`/`path`/`bitrate`/`codec`），`showVideoInfo()` 自动检测 `hasCompressedData()` 决定是否展示 overlay bar。数据随 Model 生命周期存在，不受文件列表切换影响。

**涉及文件**：
| 文件 | 改动 | 说明 |
|------|------|------|
| `model/VideoFileInfo.java` | +50 | 新增压缩后数据字段 + hasCompressedData() + clearCompressedData() |
| `view/VideoPreviewPanel.java` | 重写 | overlay bar 替代 comparisonPanel，4 列布局 + 条件着色 |
| `controller/MainController.java` | +40 | 压缩结果写入 Model + 清空列表时 reset |

### Feature 2: ffplay 视频播放

**方案**：复用 `VideoUtil.resolveCommand("ffplay")` + `ProcessBuilder` 模式，启动独立 ffplay 窗口播放。同一时间只允许一个 ffplay 实例，新播放自动关闭旧窗口，应用退出时强制关闭。

**涉及文件**：
| 文件 | 改动 | 说明 |
|------|------|------|
| `util/VideoUtil.java` | +70 | checkFfplayAvailable() + playVideo(File) + playVideo(File,int,int) |
| `view/VideoPreviewPanel.java` | +30 | ▶ 播放按钮（原始视频 + 压缩后视频） |
| `controller/MainController.java` | +100 | 播放方法 + 右键菜单 + 进程生命周期管理 |
| `dist/standalone/ffmpeg/bin/ffplay.exe` | +140MB | 捆绑到免安装版 |

### 关键设计决策

1. **对比数据放 Model 而非 View**：数据与 Model 同生命周期，切换文件时 View 只是重新渲染已持久化的数据，不会丢失
2. **overlay bar 样式对齐 PreviewPanel**：半透明白底 + 圆角 + 4 列 GridLayout + 条件着色，视觉一致性
3. **ffplay 单实例策略**：`playVideoFile()` 先 `killFfplayProcess()` 再启动新进程，防止多窗口叠加
4. **播放线程非 daemon**：`"FFplay-Thread"` 命名线程调用 `process.waitFor()` 等待 ffplay 退出，不做 daemon 避免 JVM 强制终止
5. **ffplay.exe 不纳入 Git**：已通过 .gitignore 排除，仅在分发 ZIP 中包含

### 测试结果

- 编译：`mvn compile` BUILD SUCCESS（43 源文件）
- 测试：`mvn test` — 82 tests, 0 failures, 0 errors（12 skipped = FFmpeg 集成测试）
- 运行：应用正常启动，视频模式可用

### 免安装版更新

| 组件 | 大小 |
|------|------|
| JRE | 117MB |
| ffmpeg.exe | 138MB |
| ffprobe.exe | 138MB |
| ffplay.exe | **140MB** ← 新增 |
| JAR | 2MB |
| **解压后合计** | **~535MB** |

### 下一步

1. 集成测试：在安装 FFmpeg 的环境中实测视频压缩全流程
2. 打包免安装版 ZIP：`dist/standalone/` → `NCHU-Compressor-免安装版.zip`
3. 混合文件列表：支持图片 + 视频在同一列表中（当前为模式分离）

---

### #8 VideoPreviewPanel 初始化顺序导致 NPE 崩溃

**现象**：应用启动时在 EDT 中抛出 NullPointerException，堆栈指向 `VideoPreviewPanel.createInfoPanel(160)` → `Container.addImpl`，应用无法启动。

**原因**：`VideoPreviewPanel` 构造函数中执行顺序为：
```java
emptyPanel = createEmptyPanel();
infoPanel = createInfoPanel();          // ← 内部引用 this.comparisonPanel
comparisonPanel = createComparisonPanel();  // ← 字段的初始化在 infoPanel 之后
```
`createInfoPanel()` 内部执行 `panel.add(comparisonPanel, BorderLayout.SOUTH)`，但此时 `comparisonPanel` 字段尚未赋值（为 `null`），导致 `Container.addImpl()` 抛出 NPE。编译器无法检测此类初始化顺序问题——字段默认为 `null`，只有运行时才暴露。

**解决**：将 `comparisonPanel = createComparisonPanel()` 移到 `createEmptyPanel()` 和 `createInfoPanel()` 之前。

**涉及文件**：`view/VideoPreviewPanel.java`（+3/-1）

**教训**：构造函数中调用实例方法时，如果这些方法依赖其他实例字段，必须确保字段的赋值在这些方法调用之前完成。编译器对这类初始化顺序问题零检测，只能靠人工审查或运行时测试发现。

---

### #9 EDT 阻塞导致视频模式切换和播放卡顿/无响应

**现象**：
1. 点击播放按钮后 UI 冻结 2+ 秒，ffplay 窗口才弹出
2. 首次切换到视频模式时界面无响应 ~10 秒
3. 视频压缩失败率极高，且错误信息仅显示 "非零退出码: X"，无具体原因

**原因**（三层问题）：
1. **播放 EDT 阻塞**：`killFfplayProcess()` 含 `waitFor(2, SECONDS)` 同步等待，直接写在按钮 ActionListener 中执行，阻塞 EDT
2. **模式切换 EDT 阻塞**：`checkFfmpegAvailable()` 首次调用时启动 2 个子进程（ffmpeg + ffprobe），各等 5 秒超时判断，全在 EDT 上执行
3. **压缩错误信息被丢弃**：`executeCompress()` 只解析 stderr 进度行，FFmpeg 报错行全被丢弃；stdout 缓冲区满导致 FFmpeg 管道死锁

**解决**：
| 问题 | 修复 | 涉及文件 |
|------|------|---------|
| 播放阻塞 | 所有阻塞操作（checkFfplayAvailable / killFfplayProcess / playVideo / waitFor）移入 "FFplay-Thread" 后台线程 | `controller/MainController.java` |
| 模式切换阻塞 | 启动时 "FFmpeg-Detection-Thread" 后台预检测 FFmpeg/FFplay，缓存热后 EDT 调用瞬间返回 | `controller/MainController.java` |
| 压缩无错误信息 | stderr 循环保留最后 5 行非进度输出拼入异常消息；新增 stdout 排空线程防死锁 | `service/VideoCompressUtil.java` |

**涉及文件**：`controller/MainController.java`（~40行）、`service/VideoCompressUtil.java`（~15行）

**教训**：
1. **任何可能阻塞的调用都不能出现在 EDT 上** — `Process.waitFor(timeout)` 看似有超时就没事，但它同步阻塞调用线程，EDT 上每一毫秒都是致命
2. **子进程 stderr 可能包含救命信息** — 只解析格式已知的行（进度），把其余行全丢弃，等于蒙着眼睛调试。Ring buffer 是最小代价保留诊断信息的方法
3. **管道缓冲区满是一个隐匿杀手** — FFmpeg stdout 如果没人读，管道缓存写满后 FFmpeg 阻塞在 `write()`，看起来像"卡死"。stdout 即使不用也得排空

---

### #10 同一视频多次压缩时输出文件冲突

**现象**：第一次压缩成功（如 `video.mp4` → `video_compressed.mp4`），第二次压缩同一视频直接失败，提示「输出文件已存在，请启用覆盖或修改命名规则」。

**原因**：`VideoCompressConfig.overwrite` 默认值为 `false`，`VideoCompressService.compress()` 在启动 FFmpeg 之前做预检：输出文件存在 + overwrite=false → 直接返回失败。即使 FFmpeg 命令中有 `-y` 标志也没机会执行。此外 `VideoParamPanel` 复选框标签写的是「覆盖原文件」，但视频模式**永远不会覆盖原文件**（总有 `_compressed` 后缀），标签有误导性。

**解决**：
| 改动 | 说明 |
|------|------|
| `VideoCompressConfig.overwrite` 默认值 `false` → `true` | 允许重复压缩同一视频 |
| `buildFfmpegCommand()` 中 `-y` 改为条件化（仅 overwrite=true 时添加） | 行为与配置一致 |
| `VideoParamPanel` 复选框标签改为「覆盖已存在的输出文件」+ 默认选中 | 语义准确 + UX 默认合理 |

**涉及文件**：`model/VideoCompressConfig.java`、`service/VideoCompressUtil.java`、`view/VideoParamPanel.java`（共 +8/-5）

**教训**：
1. 默认值选择应从用户最常见场景出发 — 用户重复压缩同一个视频调参是高频操作，overwrite 应默认开启
2. UI 标签在不同模式下可能含义不同 — 图片模式「覆盖原文件」成立（可配置 suffix=""），视频模式永远有后缀，标签必须区分

---

### #11 视频缩放后编码失败（force_original_aspect_ratio → 奇数宽高）

**现象**：将分辨率从「原始」改为任意其他值（480p/720p/1080p/4K）后，压缩失败。

**原因**：`scale=W:H:force_original_aspect_ratio=decrease` 在竖屏视频（如手机拍摄的 1080×1920）上，自动计算的宽度可能为奇数（如 405px）。H.264/VP9 的 yuv420p 像素格式要求宽高均为偶数，奇数 → FFmpeg 编码失败，报 "width not divisible by 2"。

**解决**：将 scale 滤镜改为 `scale=W:-2`。`-2` 是 FFmpeg 内置占位符，含义为「按宽高比自动计算该维度，并向下取偶数」（即 `trunc(oh*a/2)*2`）。目标分辨率宽度（854/1280/1920/3840）本身均为偶数，无需额外处理。

**涉及文件**：`service/VideoCompressUtil.java`（+5/-3）

**教训**：
1. **视频编码对尺寸有硬约束** — yuv420p 要求宽高为偶数（色度子采样 4:2:0 导致 UV 平面 = 亮度平面的一半）。任何自动计算的维度都必须保证满足此约束
2. **FFmpeg 的 `-2` 比手动表达式更可靠** — 比自己写 `trunc(oh*a/2)*2` 简洁且不易出错
3. **测试必须覆盖竖屏视频** — 手机拍摄的竖屏视频（9:16）是常见场景，开发时容易只测横屏（16:9）而漏掉此 case

---

### #12 视频播放无响应 — ffplay stdout 管道死锁

**现象**：点击 ▶ 播放按钮后应用无响应，ffplay 窗口不弹出。之前已修复两次 EDT 阻塞问题（#9），但播放仍然卡死。

**原因**：`VideoUtil.playVideo()` 启动 ffplay 子进程后从不读取 stdout。ffplay 持续向 stdout 输出初始化/SDL 调试信息，当 OS 管道缓冲区（4-64KB）写满后，ffplay 的 `write()` 系统调用阻塞 → 进程挂起 → Java 端 `waitFor()` 永远等不到返回 → `"FFplay-Thread"` 后台线程永久死锁。

根因与 issue #9 中修过的 `VideoCompressUtil.executeCompress()` stdout 死锁完全一致（commit `06d1741`），当时只修了压缩端，漏了播放端。

**解决**：在 `playVideo()` 中添加 `FFplay-Stdout-Drainer` 守护线程，持续排空进程 stdout。`setDaemon(true)` 确保不阻止 JVM 退出。

**涉及文件**：`util/VideoUtil.java`（+21/-1）

**教训**：
1. **子进程管道死锁是系统性风险** — 任何 `ProcessBuilder.start()` + `waitFor()` 的组合，如果没有读取 stdout/stderr，都可能在输出量超过管道缓冲时死锁。修了一处，就要排查所有同类代码
2. **守护线程是最佳排空方式（JDK 8 下）** — JDK 9+ 的 `Redirect.DISCARD` 更优雅，但 JDK 8 约束下 daemon thread 是最简洁方案：不阻止退出、不占用资源、自动回收
3. **同一类 bug 反复出现说明缺少通用抽象** — 如果以后还要启动更多 ffmpeg 子进程，应该把管道排空逻辑提取为 `ProcessUtil.drainAsync(Process)` 公共方法

---

## 2026-07-13 · VLCJ 内嵌视频播放 + 双 Tab 布局 + 主题按钮改进

### 提交: `ef55e53 feat(view): VLCJ 内嵌视频播放 + 双 Tab 信息/预览布局 + 主题按钮显示名称`

### 需求背景

用户反馈三个问题：
1. 视频不能像图片一样直接预览 — 当前选中视频后只显示元数据 + "▶ 播放"按钮，点击弹出外部 ffplay 窗口
2. 视频信息和预览应该分为两个界面（Tab）
3. 主题按钮只显示 🎨 emoji，别人不知道那是干什么的

### 技术方案：VLCJ

选型对比：

| 方案 | 嵌入 Swing | JDK 8 兼容 | 格式支持 | 可行性 |
|------|-----------|-----------|----------|--------|
| **VLCJ 3.x** | ✅ 原生 Canvas | ✅ | 全部 | ✅ 推荐 |
| JavaFX MediaPlayer | ⚠️ JFXPanel | ❌ 缺 jfxrt.jar | 有限 | ❌ |
| ffplay HWND 重定父级 | ⚠️ JNI hack | ✅ | 全部 | ❌ 太脆弱 |

**降级策略**：VLC 运行时可用 → VLCJ 内嵌播放；不可用 → 降级到 ffplay 外部窗口（保留现有逻辑）。

### 新增依赖

```xml
uk.co.caprica:vlcj:3.12.1    (VLC 播放器 Java 绑定)
net.java.dev.jna:jna:5.13.0  (JNA，VLCJ 桥梁)
net.java.dev.jna:jna-platform:5.13.0 (平台工具)
```

### 任务清单

| 任务 | 状态 | 备注 |
|------|------|------|
| VlcUtil.java | ✅ 完成 | VLC 环境检测（对标 VideoUtil 模式），NativeDiscovery + 手动 fallback |
| VideoPlayerPanel.java | ✅ 完成 | VLCJ EmbeddedMediaPlayerComponent 内嵌播放器，完整控件（播放/暂停/进度/音量） |
| VideoPreviewPanel.java 重构 | ✅ 完成 | 单一面板 → JTabbedPane 双 Tab（🎬 视频预览 + 📋 视频信息） |
| MainController.java | ✅ 完成 | 集成 VLCJ，删除旧播放按钮绑定，保留 ffplay 降级 |
| MainFrame.java | ✅ 完成 | 主题按钮从 "🎨" 改为 "🎨 默认蓝调"，新增 updateThemeButtonText() |
| 启动脚本 | ✅ 完成 | 添加 -Djna.library.path 指向捆绑的 VLC 运行时 |
| 编译验证 | ✅ 通过 | 45 源文件 0 错误 |

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `pom.xml` | 修改 | 添加 vlcj + jna + jna-platform |
| `util/VlcUtil.java` | **新建** | VLC 环境检测工具类（128 行） |
| `view/VideoPlayerPanel.java` | **新建** | VLCJ 内嵌播放器面板（~380 行） |
| `view/VideoPreviewPanel.java` | 重构 | 拆分为双 Tab，删除播放按钮（457→295 行） |
| `view/MainFrame.java` | 修改 | 主题按钮文字 + updateThemeButtonText() |
| `controller/MainController.java` | 修改 | VLCJ 集成 + 清理旧绑定 + 模式切换清理 |
| `dist/standalone/启动.bat` | 修改 | 添加 -Djna.library.path |
| `dist/standalone/启动.vbs` | 修改 | 添加 -Djna.library.path |

### 关键设计决策

1. **VideoPlayerPanel 生命周期**：EmbeddedMediaPlayerComponent 是重量级 AWT 组件，创建一次后复用。切换视频时 stop → play 新 MRL，不重建组件。仅在退出时 `release()` 释放原生资源。

2. **进度同步用 javax.swing.Timer**：每 200ms 轮询 VLC `getTime()`/`getLength()` 更新进度条。选择轮询而非 VLC 回调，因为 Timer 天然在 EDT 上执行，无需额外线程同步。200ms 间隔人眼感知流畅。

3. **控件栏独立于视频画布**：VLCJ Canvas（AWT 重量级）在 CENTER，Swing 控件栏在 SOUTH，BorderLayout 正确处理重量级/轻量级混合。

4. **降级路径保留**：`onPlayOriginalVideo()`/`onPlayCompressedVideo()` 先检查 VLC 可用性，不可用时走 `playVideoFile()`（ffplay）。右键菜单 "▶ 播放视频" 仍然有效。

5. **主题按钮文字随主题更新**：`switchToTheme()` → `mainFrame.updateThemeButtonText(theme)`，按钮始终显示当前主题名。

### 下一步

1. 捆绑 VLC 运行时到 `dist/standalone/vlc/`
2. 集成测试：VLC 已安装/未安装两种场景下视频播放
3. 混合文件列表：图片 + 视频在同一列表中

---

### 提交: `af29d7e feat(view,util): ffmpeg 管道帧渲染降级 — VLC 不可用时仍可内嵌播放`

### 问题背景

用户反馈：导入视频后点击文件列表，预览 Tab 显示「🎬 选择视频文件播放」而非视频画面。

**根因分析**：
- 用户系统未安装 VLC → `VideoPlayerPanel.vlcUsable = false`
- `play()` 方法在 `!vlcUsable` 时直接 `return`，未更新 CardLayout 状态
- CardLayout 停留在 `CARD_EMPTY`（"选择视频文件播放"），即使用户已选择视频
- 用户看到的 UI 状态与实际状态不一致，造成困惑

### 技术方案：ffmpeg 管道帧渲染

利用已捆绑的 ffmpeg，通过管道输出 raw RGB24 原始帧，Java 端逐帧读取并渲染到 Swing 面板。

**ffmpeg 命令**：
```
ffmpeg -i input.mp4 -f rawvideo -pix_fmt rgb24 -r 15 -vf scale=640:-2 -
```

**渲染流程**：
1. `ProcessBuilder` 启动 ffmpeg，stdout 输出 raw RGB24 帧流（每帧 `w×h×3` 字节）
2. 后台读取线程持续从管道读取帧 → `DataBufferInt` 直接写 `BufferedImage`
3. `javax.swing.Timer`（~66ms = 15fps）定时取最新帧 → `repaint()`
4. `JPanel.paintComponent()` 居中缩放绘制当前帧

**引擎选择链**：VLCJ → ffmpeg 管道帧渲染 → ffplay 外部窗口

### 任务清单

| 任务 | 状态 | 备注 |
|------|------|------|
| FfmpegRenderUtil.java | ✅ 完成 | ffmpeg 管道启动 + 帧读取 + RGB→BufferedImage 转换 |
| FfmpegPlayerPanel.java | ✅ 完成 | Swing 帧渲染面板（play/pause/stop + State 状态机） |
| VideoPlayerPanel.java 修改 | ✅ 完成 | 新增 CARD_FFMPEG + ffmpeg 降级路径 + play(VideoFileInfo) 重载 |
| VideoPreviewPanel.java 修改 | ✅ 完成 | showVideoInfo 传递完整 VideoFileInfo（含 fps/分辨率） |
| MainController.java 修改 | ✅ 完成 | 播放降级链：isVlcUsable() \|\| checkFfmpegAvailable() → 内嵌 else ffplay |
| 编译验证 | ✅ 通过 | 47 源文件 0 错误 |

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `util/FfmpegRenderUtil.java` | **新建** | ffmpeg 管道帧提取工具类（140 行） |
| `view/FfmpegPlayerPanel.java` | **新建** | ffmpeg 帧渲染 Swing 面板（340 行） |
| `view/VideoPlayerPanel.java` | 修改 | 新增 CARD_FFMPEG + 降级逻辑 + play(VideoFileInfo) |
| `view/VideoPreviewPanel.java` | 修改 | showVideoInfo 调用 play(info) 而非 play(file) |
| `controller/MainController.java` | 修改 | 播放降级链扩展 |

### 关键设计决策

1. **DataBufferInt 直接写入**：`FfmpegRenderUtil.rgbToImage()` 通过 `image.getRaster().getDataBuffer()` 拿到 `DataBufferInt`，直接操作底层 `int[]` 像素数组。TYPE_INT_RGB 的内存布局恰好是连续的 int 像素，逐索引赋值比 `setRGB()` 快约 10 倍。

2. **State 状态机**：`FfmpegPlayerPanel` 使用内部 `enum State { EMPTY, LOADING, PLAYING, PAUSED, STOPPED, ERROR }` 管理生命周期。每个状态对应不同的 UI 表现（按钮文字/颜色/控制栏可见性），避免了布尔标志的组合爆炸。

3. **帧输出与 Timer 解耦**：后台线程全速读取 ffmpeg 帧（存入 `volatile currentFrame`），EDT Timer 按 15fps 定时 `repaint()`。两者通过 volatile 字段通信，无需锁。Timer 只取最新帧，不追帧 — 丢帧不影响流畅度。

4. **限制接受**：ffmpeg 管道方案无音频输出、不支持精确 Seek。这些是合理的取舍 — 预览场景的重点是视觉参考，音频和精确进度交给外部 ffplay 处理。

### 下一步

- 捆绑 VLC 运行时到 `dist/standalone/vlc/`（让有 VLC 的系统获得带音频的完整播放体验）
- 用户测试 ffmpeg 降级播放实际效果
- 混合文件列表：图片 + 视频在同一列表中

---

## 2026-07-13 · Bug 修复：ffmpeg 管道播放速度过快

### 提交: `5df262a fix(util,view): 修复 ffmpeg 播放速度过快 — 使用原视频帧率 + -re 实时输出`

### 问题现象

用户反馈：ffmpeg 管道帧渲染播放视频时速度异常快，30fps 视频在 2-3 秒内"快进"播完。

### 根因分析

原实现有两个问题：
1. **帧率硬编码为 15fps**：`FfmpegRenderUtil` 中 `PREVIEW_FPS = 15` 固定值，`-r 15` 传递给 ffmpeg。但对于 24fps/30fps/60fps 的视频，解码速度远超显示帧率
2. **缺少 `-re` 标志**：ffmpeg 默认以最快速度解码输出，不加 `-re`（read input at native frame rate）时管道帧流会瞬间涌出

### 修复内容

| 文件 | 改动 |
|------|------|
| `FfmpegRenderUtil.java` | 删除固定 `PREVIEW_FPS(15)`，`startFrameStream()` 新增 `fps` 参数；ffmpeg 命令新增 `-re` 标志 |
| `FfmpegPlayerPanel.java` | Timer 间隔用原视频 fps 动态计算（`1000/fps`），不再固定 66ms |

### 关键设计

- **`-re` 标志的作用**：告诉 ffmpeg 以原视频的帧率节奏读取输入文件，配合 `-r <fps>` 控制输出帧率，实现实时节奏输出
- **Timer 间隔动态化**：不再假设所有视频都是 15fps，而是根据视频元数据的实际帧率计算刷新间隔

---

## 2026-07-13 · Bug 修复：VLC 检测 32 位 JNA 库名修正

### 提交: `c7d1204 fix(util,view): 修复 VLC 检测 — 32位 JNA 库名修正 + 捆绑 32位 VLC 运行时`

### 问题现象

VLC 已安装但 `VlcUtil.checkVlcAvailable()` 返回 false，内嵌播放不可用。

### 根因分析

`NativeLibrary.getInstance("vlc")` 在 Windows 上找不到 DLL。Windows 上 VLC 的 JNA 库名是 `libvlc`（对应 `libvlc.dll`），不是 `vlc`。此外，VLC 3.x 还需要加载 `libvlccore` 作为依赖。

### 修复内容

| 文件 | 改动 |
|------|------|
| `VlcUtil.java` | `NativeLibrary.getInstance("libvlc")` 替代 `"vlc"`；新增 `libvlccore` 搜索路径 |
| `dist/standalone/启动.bat` | 添加 VLC 目录到 `PATH` + `java.library.path` |
| `dist/standalone/启动.vbs` | 同上，设置环境变量 |
| `.gitignore` | 排除 `dist/standalone/vlc/`（128MB 运行时） |

---

## 2026-07-13 · Bug 修复：getVlcVersion() 遗留 JNA 库名

### 提交: `b0ce3ee fix(util): 修复 getVlcVersion() 中遗留的 JNA 库名 vlc → libvlc`

### 问题

`getVlcVersion()` 方法中使用 `NativeLibrary.getInstance("vlc")` 获取版本，与 `checkVlcAvailable()` 使用了不同的库名。前一个提交只修了检测逻辑，漏了版本获取方法。

### 修复

1 行改动：`"vlc"` → `"libvlc"`。

**教训**：代码中存在重复的魔法字符串时，应抽取为常量。`"libvlc"` 在 `VlcUtil` 中出现了 3 处，后续应统一为 `private static final String VLC_LIB_NAME = "libvlc"`。

---

## 2026-07-13 · 新功能：视频多版本批量导出

### 提交: `27b4e6b feat(model,view,controller): 视频多版本批量导出 — 同文件多分辨率/帧率/画质对比`

### 需求背景

同一视频文件需要导出多个版本进行对比 — 例如同一段素材同时输出 480p/720p/1080p 三个分辨率，或 CRF 20/23/28 三个质量级别，方便对比画质差异。

### 设计方案

在 `VideoCompressConfig` 中新增 `VariantPreset` 内部类，每个变体独立覆盖 resolution / fps / crf / audio / format 五个维度。

```
VideoCompressConfig
└── VariantPreset（内部类）
    ├── resolutionMode（覆盖分辨率）
    ├── fpsMode（覆盖帧率）
    ├── crf（覆盖质量）
    ├── audioMode（覆盖音频）
    ├── outputFormat（覆盖格式）
    ├── buildSuffix()（生成描述性后缀）
    └── mergeWith(base)（合并全局配置）
```

### 任务清单

| 任务 | 状态 | 备注 |
|------|------|------|
| VariantPreset 内部类 | ✅ 完成 | 5 字段 + buildSuffix() + mergeWith() |
| VideoParamPanel 变体列表 UI | ✅ 完成 | 变体添加/删除/编辑面板 |
| MainController 批量导出调度 | ✅ 完成 | 遍历变体 × 遍历文件 → 逐个压缩 |
| CompressResult 批量标记 | ✅ 完成 | 新增 variantLabel 字段区分同一文件的不同变体 |
| 编译验证 | ✅ 通过 | 5 文件改动，445 行新增 |

### 涉及文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `model/VideoCompressConfig.java` | 修改 | 新增 `VariantPreset` 内部类（85 行） |
| `view/VideoParamPanel.java` | 修改 | 变体列表 UI + 添加/删除/编辑（257 行改动） |
| `controller/MainController.java` | 修改 | 批量导出调度逻辑（103 行改动） |
| `model/CompressResult.java` | 修改 | 新增 `variantLabel` 字段 |

### 文件命名规则

变体后缀示例：`_720p_30fps_crf23.mp4`、`_480p_24fps_crf30_noaudio_mp4`

编码规则：分辨率 + 帧率 + CRF + (移除音频时标注 noaudio) + (格式变化时标注扩展名)

---

## 2026-07-13 · Bug 修复：批量导出复选框不可见

### 提交: `7a672d3 fix(view): 批量导出复选框不可见 — 将复选框移到折叠面板外部`

### 问题现象

"启用批量导出"复选框在折叠面板（CollapsiblePanel）内部，面板默认折叠时用户完全看不到该选项。

### 修复

将复选框从折叠面板内部移到外部，始终可见。折叠面板仅包裹变体列表区域，复选框独立放置在面板顶部。

---

## 2026-07-13 · 新功能：批量变体增加音频/格式维度

### 提交: `437f5a3 feat(model,view): 批量导出变体增加音频/格式维度 — 每个变体独立控制音频和输出格式`

### 需求

批量导出时每个变体不仅需要独立的分辨率/帧率/CRF，还需要独立控制：
- **音频模式**：保留音频 or 移除音轨（静音版本）
- **输出格式**：MP4 / WebM / AVI / MOV / MKV

### 改动

| 文件 | 改动 |
|------|------|
| `VideoCompressConfig.java` | `VariantPreset` 新增 `audioMode` 和 `outputFormat` 字段；`buildSuffix()` 增加音频和格式标注 |
| `VideoParamPanel.java` | 变体编辑面板新增音频下拉框（保留/移除）+ 格式下拉框（5 种格式） |

---

## 2026-07-13 · Bug 修复 + 功能增强（本轮，待提交）

> 以下为 2026-07-13 下午完成的改动，共 9 个文件，尚未提交。

### 1. VLC 捆绑版插件路径修复（三层防护）

**现象**：捆绑版 VLC（`dist/standalone/vlc/`）被正确检测到，`checkVlcAvailable()` 返回 true，但内嵌播放无音频、无进度条、无音量控制。

**根因**：VLC 启动时找不到 `plugins/` 目录。VLCJ 3.x 需要显式传入 `--plugin-path` 参数给 libvlc，仅设置 JNA 库加载路径不足以让 VLC 找到它的插件模块（音频解码器、视频滤镜等）。

**修复（三层防护）**：

| 层 | 位置 | 机制 |
|----|------|------|
| 1 | `VlcUtil.java` | 新增 `findBundledVlc()` 探测 `dist/standalone/vlc/`（支持 dev 和 dist 两种目录结构）；新增 `getVlcPluginsPath()` 缓存插件路径 |
| 2 | `VideoPlayerPanel.java` | `EmbeddedMediaPlayerComponent` 匿名子类覆写 `onGetMediaPlayerFactoryArgs()` 传入 `--plugin-path=<plugins>` |
| 3 | `dist/启动.bat` / `启动.vbs` | 设置 `VLC_PLUGIN_PATH` 原生环境变量 + `-Djna.library.path` |

**涉及文件**：

| 文件 | 改动 | 行数 |
|------|------|------|
| `util/VlcUtil.java` | 新增 `findBundledVlc()` + `getVlcPluginsPath()` + 缓存字段 | +70 |
| `view/VideoPlayerPanel.java` | `onGetMediaPlayerFactoryArgs()` 覆写传入 `--plugin-path` | +18 |
| `dist/启动.bat` | VLC 检测块：PATH + VLC_PLUGIN_PATH + jna.library.path | +15 |
| `dist/启动.vbs` | VLC 检测块：Process 环境变量设置 | +14 |

### 2. 单文件视频压缩：覆盖自动编号

**现象**：单文件压缩时，若目标文件夹已有同名文件且未勾选"覆盖"，压缩直接失败。

**根因**：`VideoCompressService.compress()` 只检查 `outputFile.exists()`，但没有自动编号逻辑。而图片压缩的 `CompressService` 已有 `FileUtil.generateUniqueFilename()` 自动编号。

**修复**：`VideoCompressService.java` 第 55-59 行，非覆盖模式下调用 `FileUtil.generateUniqueFilename()` 生成 `demo(1).mp4`、`demo(2).mp4` 等唯一文件名。

### 3. 批量按钮文案修复

**现象**：批量压缩按钮显示"批量导出 0 个版本"，即使已添加多个变体。

**根因**：`VideoParamPanel` 中批量复选框的 ActionListener 调用 `updateCompressButtonText(0)`，传入硬编码的 `0`。

**修复**：移除硬编码的 `0`；新增 `onVariantChanged` 回调，在变体增删时自动更新按钮文案。`MainController` 注册该回调。

### 4. 单文件压缩：输出文件名包含压缩参数

**需求**：单文件压缩的输出文件名体现压缩参数（分辨率、帧率、格式、音频、CRF），方便区分不同设置的结果。

**实现**：
- `VideoCompressConfig.buildDynamicSuffix()` 实例方法，根据自身字段生成后缀，规则与 `VariantPreset.buildSuffix()` 完全一致
- `VideoParamPanel.buildConfig()` 在构建配置时调用 `config.setSuffix(config.buildDynamicSuffix())`

**示例输出**：
| 参数 | 文件名 |
|------|--------|
| 默认 | `demo_original_origfps_crf23.mp4` |
| 720p + 30fps + CRF28 | `demo_720p_30fps_crf28.mp4` |
| 480p + 移除音频 + WebM | `demo_480p_origfps_crf23_noaudio_webm` |

### 本轮改动文件总览

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `util/VlcUtil.java` | 修改 | `findBundledVlc()` + 插件路径缓存 |
| `view/VideoPlayerPanel.java` | 修改 | `--plugin-path` 传入 VLCJ 工厂 |
| `model/VideoCompressConfig.java` | 修改 | `buildDynamicSuffix()` 方法 |
| `service/VideoCompressService.java` | 修改 | 覆盖自动编号（`generateUniqueFilename`） |
| `view/VideoParamPanel.java` | 修改 | 动态后缀调用 + `onVariantChanged` 回调 |
| `controller/MainController.java` | 修改 | 注册 `onVariantChanged` 回调 |
| `dist/启动.bat` | 修改 | VLC 检测块 |
| `dist/启动.vbs` | 修改 | VLC 检测块 |
| `dist/image-compressor-1.0.0.jar` | 更新 | 重新打包 |

---

## 问题日志（长期 · 续）

### #8 VLC 捆绑版无音频/无控件

**现象**：捆绑版 VLC 被检测到，内嵌播放器能显示画面，但无音频输出、无进度条、无音量控制滑块。

**原因**：VLC 启动时找不到 `plugins/` 目录。仅设置 JNA 的 `NativeLibrary.addSearchPath()` 加载 `libvlc.dll` 不够 — libvlc 启动后还需要加载 `plugins/` 下的音频解码器、视频滤镜等模块。VLCJ 3.x 需要通过 `--plugin-path` 参数告知 libvlc 插件目录位置。

**解决**：三层防护 —
1. Java 层：`EmbeddedMediaPlayerComponent.onGetMediaPlayerFactoryArgs()` 传入 `--plugin-path`
2. 启动脚本层：设置 `VLC_PLUGIN_PATH` 原生环境变量
3. 自动探测层：`VlcUtil.findBundledVlc()` 自动发现捆绑版 VLC 并缓存插件路径

**涉及文件**：`VlcUtil.java`、`VideoPlayerPanel.java`、`dist/启动.bat`、`dist/启动.vbs`

**教训**：JNA 库加载（`NativeLibrary`）只解决了 Java↔Native 的函数调用桥梁，但 VLC 是一个模块化架构 — `libvlc.dll` 只是入口，实际解码/渲染由 `plugins/` 下的 200+ 个模块完成。只加载入口 DLL 不告诉它插件在哪，等于给了钥匙但没告诉门在哪。

### #9 视频单文件压缩重名直接失败

**现象**：单文件压缩时，若输出目录已有同名文件，非覆盖模式下直接返回失败，不会像图片压缩那样自动编号。

**原因**：`VideoCompressService` 是新写的服务类，覆盖检测逻辑来自 `VideoCompressUtil.generateOutputFile()`，但缺少 `FileUtil.generateUniqueFilename()` 自动编号调用。图片压缩的 `CompressService` 早已包含此逻辑，视频服务开发时遗漏了对齐。

**解决**：在 `VideoCompressService.compress()` 中添加与图片压缩一致的非覆盖自动编号逻辑。

**涉及文件**：`VideoCompressService.java`

**教训**：相似功能的服务类应保持行为一致。视频压缩服务开发时参考图片压缩服务的已有逻辑可避免此类遗漏。后续可考虑抽取公共的「文件输出策略」模块。

### #10 批量导出按钮显示"0 个版本"

**现象**：已添加多个导出变体，但压缩按钮始终显示"批量导出 0 个版本"。

**原因**：批量复选框的 `ActionListener` 中硬编码 `updateCompressButtonText(0)`，无论实际变体数量多少都传 0。

**解决**：移除硬编码调用；新增 `onVariantChanged` 回调机制，变体增删时自动触发按钮文案更新，传入实际变体数量。

**涉及文件**：`VideoParamPanel.java`、`MainController.java`

**教训**：硬编码的占位值容易在后续开发中被遗忘。初期可以用硬编码快速跑通流程，但必须在 TODO 注释中标记，并在功能完成后清理。

### #11 视频元数据全部显示"未知" — FFmpeg 未安装/未被发现

**现象**：导入视频文件后，视频信息 Tab 中时长、分辨率、编码、帧率、比特率全部显示"未知"，仅文件名和大小正常。

**原因**：分两层 —
1. **环境层**：系统未安装 FFmpeg（全盘搜索无 `ffprobe.exe`）。Java 没有内置视频元数据解析能力，必须依赖 ffprobe 外部工具。
2. **代码层**：`VideoUtil.resolveCommand()` 仅检查系统属性 `ffmpeg.bin.path` 和系统 PATH。若 ffprobe 由 IDE 安装（如 TRAE SOLO CN 放入其 bin 目录），从 IDE 内启动的 App 能找到（继承 IDE 的 PATH），但从命令行 `mvn exec:java` 启动的 App 找不到（继承系统 PATH）。

**解决**：
1. `VideoUtil.java` — 新增 `autoDiscoverFfmpegPath()` 方法，扫描 10+ 常见 Windows 安装目录（`FFMPEG_HOME`、winget、Chocolatey、Scoop、`C:\ffmpeg\` 等），结果缓存供 `resolveCommand()` 使用
2. `VideoPreviewPanel.java` — 元数据全空且 FFmpeg 不可用时，显示橙色警告条：「FFmpeg 未安装，无法读取视频元数据」+ 安装指引
3. 推荐用户运行 `winget install ffmpeg` 一键安装

**涉及文件**：`VideoUtil.java`、`VideoPreviewPanel.java`

### #12 文件列表「清空」按钮显示空白

**现象**：文件列表右上角清空按钮只显示一个空白方块，无任何文字。

**原因**：按钮使用 emoji `🗑` + `Segoe UI Emoji` 14px 字体 + `setPreferredSize(24, 24)` 固定尺寸。emoji 在该字体/尺寸组合下渲染为空白，24x24 也太小无法容纳文字。

**解决**：改为纯文字 `"清空"`，使用 `ThemeUtil.FONT_SMALL` 统一字体，移除 `setPreferredSize` 让按钮自适应文字大小。

**涉及文件**：`FileListPanel.java`

**教训**：Swing 对 emoji 的渲染支持不稳定，依赖特定字体。UI 按钮文字优先使用纯文本，emoji 仅用于大尺寸图标或装饰。

---

**教训**：依赖外部 CLI 工具的 Java 应用，**不能假设工具一定在 PATH 上**。`ProcessBuilder` 继承 JVM 进程的 PATH，而 JVM 继承启动它的 shell/IDE 的 PATH — 这导致同一份代码在不同启动方式下行为不一致。最佳实践：(1) 文件系统级别的自动发现（遍历常见目录），(2) 允许用户通过系统属性/配置文件指定工具路径，(3) 不可用时给出清晰的安装指引而非静默失败。

---

## 2026-07-17 · 预览区空状态美化

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| 预览区空状态添加相机图标 | ✅ 完成 | `PreviewCanvas.drawEmptyState()` 居中显示 48×48 camera.svg |

### 涉及文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `view/PreviewCanvas.java` | 修改 | 添加 `emptyIcon` 字段（FlatSVGIcon 48×48），`drawEmptyState()` 中文字上方居中绘制相机图标 |

---

## 2026-07-17 · EXIF 信息展示功能

**分支**: `feat/exif-info`

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| ImageExifUtil 工具类 | ✅ 完成 | 基于 metadata-extractor 2.18.0（已在 pom.xml 中） |
| ImageFileInfo 模型扩展 | ✅ 完成 | +`exifData` 字段 + getter/setter |
| PreviewPanel 双 Tab 重构 | ✅ 完成 | CENTER 从直接 add(canvas) → JTabbedPane「图片预览 | 图片信息」 |
| Controller EXIF 读取联调 | ✅ 完成 | `onFileSelected()` 后台线程读取 EXIF → `showImageInfo()` |

### 展现的 EXIF 字段

相机品牌/型号、拍摄时间、曝光时间、光圈值、ISO、焦距、闪光灯、图像尺寸、GPS 位置、处理软件、版权信息。EXIF 缺失时显示 "—"。

### 涉及文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `util/ImageExifUtil.java` | 新增 | EXIF 读取 + `ExifData` 内部类，异常安全 |
| `model/ImageFileInfo.java` | 修改 | +`exifData` 字段 |
| `view/PreviewPanel.java` | 修改 | CENTER → JTabbedPane 双 Tab，+297 行 |
| `controller/MainController.java` | 修改 | `onFileSelected()` 后台读取 EXIF |

---

## 2026-07-17 · 分支合并 — feat/exif-info + feat/custom-filename → master

### 合并概述

两个独立特性分支合并到 master，编译验证通过并推送到 GitHub。

### 合并清单

| 分支 | 功能 | 提交数 | 涉及文件 | 合并方式 |
|------|------|--------|---------|---------|
| `feat/exif-info` | 图片 EXIF 信息展示 | 1 | util/ImageExifUtil.java(新), model/ImageFileInfo.java, view/PreviewPanel.java, controller/MainController.java | Fast-forward |
| `feat/custom-filename` | 图片/视频输出文件自由命名 | 1 | model/{CompressConfig,VideoCompressConfig,AppConfig}.java, view/{ParamPanel,VideoParamPanel}.java, service/{CompressService,VideoCompressUtil}.java, controller/MainController.java | ort merge |

### 合并后 Git 历史

```
05fb589 feat(model,service,view,controller): 图片/视频输出文件自由命名功能
ea2fbde feat(view,util,model,controller): 图片 EXIF 信息展示 — 双 Tab 预览+信息面板
a17aa60 docs(project): 同步进度 — EXIF 信息展示功能（feat/exif-info）
```

### 编译验证

`mvn package -DskipTests` BUILD SUCCESS，0 错误 0 警告。两个功能独立开发、独立分支、合并无冲突。

---

## 2026-07-17 · 高优先级 Bug 批量修复（项目审查第 1 轮）

### 提交: `8afce34 fix(view,util): 批量修复高优先级 Bug — 主题适配 + 竞态修复 + 可访问性提升`

### 修复清单

| # | 问题 | 严重性 | 文件 | 修复方式 |
|---|------|--------|------|---------|
| 1 | ResultDialog 完全未主题化 | 🔴 深色主题不可读 | [ResultDialog.java](src/main/java/com/nchu/imagecompress/view/ResultDialog.java) | 颜色→ThemeUtil，字体→FONT_*，emoji→FlatSVGIcon，fileSize→FileUtil |
| 2 | createGradientButton 实例变量竞态 | 🔴 所有按钮共享状态 | [MainFrame.java](src/main/java/com/nchu/imagecompress/view/MainFrame.java) | 实例变量→putClientProperty(PROP_HOVERED/PRESSED) |
| 3 | PreviewCanvas 棋盘格硬编码 | 🟡 深色主题视觉不适 | [PreviewCanvas.java](src/main/java/com/nchu/imagecompress/view/PreviewCanvas.java) | LIGHT_CELL/DARK_CELL→ThemeUtil.BG_CARD/BG_HOVER |
| 4 | formatFileSize() 5 处重复实现 | 🟡 DRY 违规 | [PreviewPanel.java](src/main/java/com/nchu/imagecompress/view/PreviewPanel.java), [ResultDialog.java](src/main/java/com/nchu/imagecompress/view/ResultDialog.java) | 全部改为 FileUtil.formatFileSize() |
| 5 | 分段控件 JLabel 不可键盘访问 | 🟡 可访问性缺失 | [PreviewPanel.java](src/main/java/com/nchu/imagecompress/view/PreviewPanel.java) | JLabel+MouseListener→JToggleButton+ButtonGroup |
| 6 | getCurrentTheme() Color.equals 推断 | 🟡 潜在误判 | [ThemeUtil.java](src/main/java/com/nchu/imagecompress/util/ThemeUtil.java) | 新增 currentThemeCache 字段，O(1) 直接返回 |

### 涉及文件

| 文件 | 改动 | 行数 |
|------|------|------|
| `view/ResultDialog.java` | 全面主题化 | +82/-82 |
| `view/MainFrame.java` | 渐变按钮竞态修复 | +32/-20 |
| `view/PreviewCanvas.java` | 棋盘格主题感知 | +7/-4 |
| `view/PreviewPanel.java` | 分段控件+TFileSize去重 | +69/-57 |
| `util/ThemeUtil.java` | getCurrentTheme缓存 | +23/-23 |

### 编译验证

`mvn compile` BUILD SUCCESS，0 错误。全部 7 套主题下 UI 验证通过。

---

## 2026-07-17 · 中优先级改进（项目审查第 2 轮）

### 提交: `6631299` feat(controller,view): 快捷键 + 状态栏自适应 + Toast/SplashScreen

### 提交: `1eb7dd3` feat(view): 文件列表搜索/筛选/排序功能

### 改进清单

| # | 问题 | 文件 | 修复方式 |
|---|------|------|---------|
| 7 | 缺少压缩/取消/全选快捷键 | [MainController.java](src/main/java/com/nchu/imagecompress/controller/MainController.java) | JRootPane InputMap: Ctrl+Enter(压缩), Escape(取消), Ctrl+A(全选) |
| 8 | 状态栏进度条固定 300px | [StatusBar.java](src/main/java/com/nchu/imagecompress/view/StatusBar.java) | 移除 setPreferredSize → BorderLayout.CENTER 自适应 |
| 9 | Toast currentOffsetY 未使用 | [ToastNotification.java](src/main/java/com/nchu/imagecompress/view/ToastNotification.java) | 移除未使用字段（队列已保证不重叠） |
| 10 | SplashScreen 硬编码字体 | [SplashScreen.java](src/main/java/com/nchu/imagecompress/view/SplashScreen.java) | 字体改为 ThemeUtil.FONT_* |
| 11 | 文件列表无搜索/排序 | [FileListPanel.java](src/main/java/com/nchu/imagecompress/view/FileListPanel.java) | 新增搜索栏 + 排序下拉(原始/名称/大小)，主数据源 allFiles 与显示模型分离 |

### 涉及文件

| 文件 | 改动 | 行数 |
|------|------|------|
| `controller/MainController.java` | 全局快捷键绑定 | +52 |
| `view/StatusBar.java` | 进度条自适应 | +1/-3 |
| `view/ToastNotification.java` | 清理未使用代码 | +2/-5 |
| `view/SplashScreen.java` | 字体 ThemeUtil | +5/-5 |
| `view/FileListPanel.java` | 搜索/排序/去重 | +116/-18 |

### 编译验证

`mvn compile` BUILD SUCCESS，0 错误。全部功能验收通过。

---

## 2026-07-17 · 中优先级改进（项目审查第 3 轮）

### 提交清单

| 提交 | 功能 | 涉及文件 |
|------|------|---------|
| `adb502d` | 撤销机制 — Ctrl+Z 撤销移除/清空 | MainController.java |
| `ee9ef80` | 设置对话框 — 输出目录/FFmpeg路径/主题 | SettingsDialog.java(新), MainController.java |
| `98b9c64` | 视频批量多线程并行 — ExecutorService(2线程) | MainController.java |

### 改进详情

| # | 问题 | 修复方式 |
|---|------|---------|
| 12 | 无撤销机制 | 单文件移除存储 undoRemovedFile + 清空存储 undoClearAllFiles，Ctrl+Z 触发 onUndo()，5 秒自动清除 |
| 13 | 设置对话框是空壳 | SettingsDialog: GridBagLayout 表单 + 输出目录浏览 + FFmpeg 路径 + 主题选择，ESC 关闭 |
| 14 | 视频串行压缩慢 | startSingleVideoCompress/Batch → ExecutorService 2 线程并行，synchronized 结果收集，12h 超时 |

### 编译验证

`mvn compile` BUILD SUCCESS，0 错误。

---

## 2026-07-17 · 低优先级功能实现（项目审查第 4 轮）

### 提交: `7512b21` `e1fb9e2`

| 功能 | 文件数 | 说明 |
|------|--------|------|
| 清理死代码 | 1 | 移除 AnimationUtil.java (374行零引用) |
| 图片目标大小压缩 | 5 | CompressConfig+targetSizeKB, 二分搜索(5-100,8轮), ParamPanel UI, AppConfig 持久化 |
| 视频裁剪截取 | 3 | VideoCompressConfig+start/duration, ParamPanel UI, FFmpeg -ss/-t 参数 |

### 编译验证

`mvn compile` BUILD SUCCESS，0 错误。

---

## 2026-07-17 · MainController 拆分为子控制器（项目审查第 5 轮）

### 提交: `1f48066`

### 背景

MainController.java 已达 2057 行，承担了所有图片和视频的事件分发、线程调度、UI 更新职责，职责过重。按照 MVC 架构原则，将其拆分为三个类。

### 拆分方案

```
controller/
├── MainController.java     (624 行, -70%)  ← 路由器：共享操作 + 模式调度
├── ImageController.java    (440 行, 新增)   ← 图片压缩全生命周期
└── VideoController.java    (470 行, 新增)   ← 视频压缩 + 播放
```

### 职责划分

| 类 | 职责 |
|----|------|
| MainController | 初始化、主题切换、设置、快捷键、撤销、窗口生命周期、拖拽导入、右键菜单、模式切换路由 |
| ImageController | 图片导入/去重、预览生成+EXIF、参数构建/恢复、批量压缩(CompressWorker)、单文件压缩、质量滑块防抖预览 |
| VideoController | 视频导入/元数据解析、预览(VLCJ+ffmpeg)、单/多版本压缩(ExecutorService)、ffplay 播放管理、参数持久化 |

### 回调桥梁

- `ImageController.ImageControllerCallback` — 只有 `setCompressingState(boolean)`
- `VideoController.VideoControllerCallback` — 只有 `setCompressingState(boolean)`
- MainController 实现两个接口，负责统一管理工具栏按钮启用状态

### 涉及文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `controller/ImageController.java` | 新增 | 440行，提取图片压缩全流程 |
| `controller/VideoController.java` | 新增 | 470行，提取视频压缩全流程 |
| `controller/MainController.java` | 重写 | 2057→624行(-70%)，精简为路由器+共享操作 |

### 编译验证

`mvn compile` BUILD SUCCESS，0 错误。

## 2026-07-18 · Bug 修复：预览分段控件标签不可见

### 问题日志

- **现象**：图片压缩界面右上角"原图|效果图|对比"分段标签全部不可见
- **原因**：
  1. `ThemeUtil.applyTheme()` 只有注释占位，从未调用 `FlatLightLaf.setup()`，app 运行在系统默认 L&F 下
  2. segment 面板 `paintComponent()` 完全绘制不透明背景，却 `setOpaque(false)` 告知 Swing 透明，导致 RepaintManager 跳过绘制
- **解决**：
  1. `ThemeUtil.applyTheme()` 添加 FlatLaf 初始化：`UIManager.getLookAndFeel() instanceof FlatLaf` 否则 `FlatLightLaf.setup()`
  2. `PreviewPanel`：`segment.setOpaque(false)` → `true`
  3. Compressor 版已使用 JToggleButton（键盘可访问），paintComponent 内动态设置 foreground，无需额外 repaint 修正
- **教训**：自定义绘制组件完全填充不透明背景时必须同步 `setOpaque(true)`

### 涉及文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `util/ThemeUtil.java` | 修改 | 添加 FlatLightLaf import + FlatLaf 初始化 |
| `view/PreviewPanel.java` | 修改 | segment.setOpaque(true) |
`mvn test` 90 tests, 0 failures, 1 error（仅 FFmpeg 集成测试，与重构无关）。

### 问题日志

#### #15 SplashScreen 启动空指针 NPE

**现象**：`mvn exec:java` 运行应用时报 `NullPointerException at SplashScreen.java:51`，`FONT_TITLE` 为 null，GUI 窗口无法显示。

**原因**：App.java 中 SplashScreen（第 48 行）在 `ThemeUtil.applyTheme()`（第 56 行）之前创建。`FONT_TITLE`/`FONT_SMALL`/`FONT_TINY` 是 ThemeUtil 的静态字段，只有在 `applyTheme()` → `applyPalette()` 中才被赋值。此前这些字段为 null，SplashScreen 构造函数中调用 `ThemeUtil.FONT_TITLE.deriveFont(22f)` 直接抛 NPE。

**解决**：SplashScreen 添加 `safeFont(Font, float)` 辅助方法，主题字体为 null 时回退到 `new Font("Microsoft YaHei", Font.PLAIN, size)`。

**涉及文件**：`SplashScreen.java` — 3 处字体引用改为 safeFont() 包装 + 新增 safeFont() 方法 + import java.awt.Font。

**教训**：静态字段依赖初始化顺序时，使用者必须做 null 防御。启动流程中任何在主题加载之前创建的 UI 组件都应使用安全回退字体。

---

### #16 视频裁剪压缩失败

**现象**：在视频参数面板中设置裁剪起始/时长后，压缩任务返回失败，FFmpeg 非零退出码。

**原因**（两个）：
1. **`-ss` 放在 `-i` 之前**：FFmpeg 输入 seek（`-ss` before `-i`）使用关键帧级别搜索，在某些视频格式（尤其是 B 帧较多的 H.264）下会导致不准确的起始帧，甚至编码失败。FFmpeg 官方文档建议对编码场景将 `-ss` 放在 `-i` 之后使用输出 seek。
2. **`VariantPreset.mergeWith()` 未继承裁剪参数**：批量多变体模式调用 `mergeWith()` 时创建全新 `VideoCompressConfig`，只复制了画质/分辨率/帧率/音频/格式，**遗漏了 `startTimeSeconds` 和 `durationSeconds`**，导致批量模式下裁剪参数丢失。

**解决**：
1. `VideoCompressUtil.buildFfmpegCommand()` — `-ss` 移到 `-i` 之后，生成 `ffmpeg -i input -ss START -t DURATION ...` 形式的可靠命令
2. `VideoCompressConfig.VariantPreset.mergeWith()` — 新增 `merged.setStartTimeSeconds(base.getStartTimeSeconds())` 和 `merged.setDurationSeconds(base.getDurationSeconds())`
3. `VideoCompressService.compress()` — 裁剪时用有效输出时长（原时长 − 起始 + 时长上限）取代原始时长传给 `executeCompress`，修复进度百分比计算
4. `VideoCompressUtil.formatTimeArg()` — 重写以保留小数秒精度，输出格式 `H:MM:SS.sss` 避免取整丢失

**涉及文件**：
| 文件 | 变更 |
|------|------|
| `VideoCompressUtil.java` | buildFfmpegCommand() `-ss` 移到 `-i` 后；重写 formatTimeArg() 保留毫秒精度 |
| `VideoCompressConfig.java` | VariantPreset.mergeWith() 继承 base 的裁剪参数 |
| `VideoCompressService.java` | 计算有效输出时长（考虑裁剪）传给 executeCompress() |

**教训**：
1. FFmpeg 参数顺序影响语义和执行结果 — `-ss` 在 `-i` 前/后分别对应输入 seek 和输出 seek，后者更可靠
2. 批量/合并模式的所有配置维度必须完整覆盖，新增配置字段后需检查所有工厂/合并方法
3. 带时长参数的处理方法需保留浮点精度，`(int) seconds` 强制取整会丢失毫秒信息

---

## 2026-07-18 · UI 改版：蓝韵系列 6 套主题 + 渐变背景 + UX 全面优化

**分支**: `feat/ui-bluerhyme`

### 提交: `19da799 feat(view,model,util): 新增蓝韵系列 6 套主题 + 渐变背景 + 卡片/按钮/空状态 UX 优化`

### 进度概览

| 任务 | 状态 | 备注 |
|------|------|------|
| ThemePalette 6 套蓝韵主题 | ✅ 完成 | 1 浅色 (BLUE_RHYME) + 5 暗色 (OCEAN/DARK/SUNSET/FOREST/LAVENDER) |
| Theme 枚举 + AppConfig 默认值 | ✅ 完成 | 6 新枚举值，默认改为 BLUE_RHYME |
| BackgroundPanel 渐变背景 | ✅ 完成 | 暗色主题 135° 对角线渐变，浅色主题纯色 |
| CardWrapper 单层阴影 | ✅ 完成 | 双层→单层 (0,4,12)，暗色卡片可选描边 |
| 按钮全部跟随主题 | ✅ 完成 | 主按钮发光色/次要按钮边框/模式胶囊全部 ThemeChangeListener |
| 空状态重设计 | ✅ 完成 | 虚线拖拽区 + 双行引导文字 + emoji 图标 |
| Section Header 样式 | ✅ 完成 | 表头/标题统一小号加粗三级文字色 |
| StatusBar 升级 | ✅ 完成 | 36px BG_CARD 凸起背景 |
| **Bug 修复**: 主题切换后背景不更新 | ✅ 完成 | FileListPanel/PreviewPanel/ParamPanel/VideoPreviewPanel/VideoParamPanel/VideoPlayerPanel 添加 ThemeChangeListener |
| overlay bar 硬编码白色 | ✅ 完成 | PreviewPanel/VideoPreviewPanel overlay bar 改为主题感知半透明 |
| 编译验证 | ✅ 通过 | `mvn compile` BUILD SUCCESS，54 源文件 0 错误 |

### 涉及文件 (14 个)

| 文件 | 改动 | 说明 |
|------|------|------|
| `model/ThemePalette.java` | +118 | bgGradient 字段 + 6 蓝韵工厂方法 |
| `model/Theme.java` | +22 | 6 枚举值 |
| `model/AppConfig.java` | 1 行 | 默认→BLUE_RHYME |
| `view/MainFrame.java` | +74 | BackgroundPanel + 按钮/胶囊 ThemeChangeListener + 渐变按钮发光主题化 |
| `view/FileListPanel.java` | +70 | 空状态重设计 + headerRow 字段 + ThemeChangeListener |
| `view/PreviewPanel.java` | +41 | ThemeChangeListener + overlayBar 动态色 |
| `view/ParamPanel.java` | +16 | ThemeChangeListener + 标题 section-header 化 |
| `view/VideoParamPanel.java` | +15 | ThemeChangeListener + 标题 section-header 化 |
| `view/VideoPreviewPanel.java` | +11 | ThemeChangeListener + overlayBar 动态色 |
| `view/VideoPlayerPanel.java` | +6 | ThemeChangeListener |
| `view/StatusBar.java` | +9 | 36px + BG_CARD + ThemeChangeListener |
| `util/CardWrapper.java` | +21 | 双层→单层阴影 + 可选边框 |
| `service/VideoCompressUtil.java` | +27 | — (之前分支修复) |
| `App.java` | +18 | 启动时加载已保存配置主题 |

### 关键设计决策

1. **ThemeChangeListener 模式**：主题切换时 `ThemeUtil.switchTheme()` 更新静态字段 → `fireThemeChange()` 通知所有监听器 → `FlatLaf.updateUI()` 全局刷新。组件在构造时注册监听器，lambda 中重新 `setBackground(ThemeUtil.BG_*)` 获取最新 Color 对象。

2. **BackgroundPanel 渐变计算**：使用 `Math.cos/sin(gradientAngle)` 计算渐变向量，左上方为 `bgWindowEnd`（更深色），右下方为 `bgWindow`。`bgWindowEnd == null` 时退化为纯色填充，向后兼容旧主题。

3. **overlayBar 动态颜色**：`paintComponent()` 内读取 `ThemeUtil.BG_CARD` → 取 RGB 分量 + 固定 alpha 220 → 构造半透明 Color。暗色主题自动呈现深色半透明层。

### 问题日志

#### #17 主题切换后文件列表等区域背景停留在旧颜色

**现象**：切换到暗色主题后，文件列表、参数面板、预览面板等区域背景依旧是之前主题的颜色（如白色或黑色），形成"黑块"或"白块"。

**原因**：`JComponent.setBackground(Color)` 存储的是 Color 对象的引用值，而非对 `ThemeUtil.BG_CARD` 的动态查询。`ThemeUtil.switchTheme()` 更新静态字段为新 Color 对象，但 Swing 组件仍持有旧 Color 对象引用。`FlatLaf.updateUI()` 不会覆盖非 UIResource 的显式 `setBackground()` 值。

**解决**：7 个视图面板（FileListPanel / PreviewPanel / ParamPanel / VideoPreviewPanel / VideoParamPanel / VideoPlayerPanel / MainFrame 按钮）在构造函数末尾注册 `ThemeUtil.addThemeChangeListener()`，lambda 中重新调用 `setBackground()`/`repaint()`。

**涉及文件**：6 个 view 文件，共 +40 行

**教训**：任何在构造函数中使用 `ThemeUtil.BG_*/TEXT_*/PRIMARY` 等主题令牌调用 `setBackground()`/`setForeground()` 的组件，都必须注册 ThemeChangeListener 重新应用。否则组件永远停留在第一次构造时的颜色。

---

## 2026-07-18 · Bug 修复：主题切换后文字颜色不更新（"字都看不见了"）

### 提交: `e1a759b fix(view,util): 主题切换后文字颜色不更新 → 集中式动态注册系统`

### 问题概述

用户反馈：切换主题后大量文字（按钮标签、文件名、参数标签等）在暗色主题下看不见。

### 根因分析

与 #17 背景 bug 完全同因：`setForeground(ThemeUtil.TEXT_PRIMARY)` 在构造时存储当时 Color 对象的引用值。

浅色主题 → `TEXT_PRIMARY = #1A1A2E`（近黑），`BG_CARD = #FFFFFF`（白）→ 可见 ✓  
暗色主题 → `TEXT_PRIMARY = #E8E8F0`（近白），`BG_CARD = #222240`（深蓝黑）  
但 JLabel/JButton 仍持有旧的 `#1A1A2E` → 深色背景 + 深色文字 = **完全看不见** ✗

### 修复方案：集中式动态注册系统

在 `ThemeUtil` 中新增 `WeakHashMap<JComponent, Supplier<Color>>` 注册表 + 两个公共方法：

```java
ThemeUtil.setDynamicForeground(component, () -> ThemeUtil.TEXT_PRIMARY)
ThemeUtil.setDynamicBackground(component, () -> ThemeUtil.BG_CARD)
```

`fireThemeChange()` 时遍历注册表，重新执行所有 Supplier 刷新颜色。

相比 Phase 8 的"每个面板手动写 ThemeChangeListener"方案：
- 无需将局部变量提升为字段
- 无需在每个面板维护独立的 Listener 代码
- 调用 `setDynamicForeground()` 即自动注册，零遗漏

### 影响范围（11 文件，~73 处 setForeground + ~15 处 setBackground）

| 文件 | 改动 |
|------|------|
| `util/ThemeUtil.java` | +35行：fgRegistry/bgRegistry + setDynamicForeground/setDynamicBackground + refreshDynamicColors |
| `view/VideoPreviewPanel.java` | ~12 处前景色迁移 |
| `view/VideoParamPanel.java` | ~12 处前/背景色迁移 |
| `view/FileListPanel.java` | ~11 处前景色迁移 |
| `view/MainFrame.java` | ~10 处前/背景色迁移 |
| `view/ParamPanel.java` | ~10 处前/背景色迁移 |
| `view/PreviewPanel.java` | ~14 处前/背景色迁移 |
| `view/StatusBar.java` | ~9 处前/背景色迁移 |
| `view/VideoPlayerPanel.java` | ~2 处前景色迁移 |
| `view/ThemePopup.java` | ~2 处前景色迁移 |
| `view/ToastNotification.java` | ~1 处前景色迁移 |

### 编译验证

`mvn compile` BUILD SUCCESS · 54 源文件 · 0 错误

### 后续建议

Phase 8 手动书写的 ThemeChangeListener（背景刷新）可后续简化为统一使用 `setDynamicBackground()` 注册，减少代码重复。

---

## 2026-07-19 · Bug 修复：视频裁剪压缩始终失败

### 提交: `04b15be fix(service,util,view,controller): 视频裁剪压缩失败 + Unicode tofu 双修复`

### 问题概述

用户反馈：视频裁剪压缩不管什么设置都会失败。

### 根因分析

**根因 1（最可能的元凶）— FFmpeg 路径解析不一致**：

- `VideoUtil.resolveCommand()` 有完整的自动发现机制：扫描 `FFMPEG_HOME`、winget、Chocolatey、Scoop 等
- `VideoCompressUtil.resolveFfmpegPath()` 只检查 `ffmpeg.bin.path` 系统属性，兜底返回裸 `"ffmpeg"`
- `FfmpegRenderUtil.resolveFfmpeg()` 同样简陋

后果：`VideoUtil.checkFfmpegAvailable()` 返回 `true`（通过 winget/scoop 找到了 FFmpeg），但压缩服务用裸 `"ffmpeg"` 执行 → 若该目录不在 PATH 则 ProcessBuilder 抛 IOException。

**根因 2 — `-ss` 放在 `-i` 之后（输出 seek）**：

输出 seek 会让 FFmpeg 解码所有帧再丢弃。对部分容器格式（AVI、损坏的关键帧索引），这会导致编码失败或空输出。应使用输入 seek（`-ss` 放在 `-i` 前）。

**根因 3 — 不校验 startTime 是否超过视频时长**：

当 `startTimeSeconds > durationSeconds` 时，FFmpeg 仍执行，输出空文件，校验逻辑抛出异常。

**附带问题 — 裁剪时间输入错误静默吞没**：

`NumberFormatException` 被静默 catch，用户输入 "1:30" 或 "abc" 无任何提示。

### 修复方案

| # | 文件 | 改动 | 说明 |
|---|------|------|------|
| 1 | `util/VideoUtil.java` | +15行 | 新增 getFfmpegPath() / getFfprobePath() 公共方法 |
| 2 | `service/VideoCompressUtil.java` | ±15行 | 移除独立静态字段，改用 VideoUtil.getFfmpegPath()；-ss 移到 -i 前 |
| 3 | `service/VideoCompressService.java` | +8行 | 裁剪起始时间 > 视频时长时拒绝压缩 |
| 4 | `util/FfmpegRenderUtil.java` | ±5行 | resolveFfmpeg() 委托 VideoUtil.getFfmpegPath() |
| 5 | `view/VideoParamPanel.java` | ±12行 | NumberFormatException 弹 Toast；负数/零值校验 |
| 6 | `controller/VideoController.java` | +2行 | buildConfig() 返回 null 时安全退出 |

### 编译验证

`mvn compile` BUILD SUCCESS · 54 源文件 · 0 错误

---

## 2026-07-19 · Bug 修复：Unicode 字符显示为小方格（tofu）

### 提交: `04b15be`（同上提交，双修复合并）

### 问题概述

用户反馈："一些文字前面始终有小方格"。经典 tofu（豆腐块）问题：字符不在当前渲染字体中 → Java 渲染为空心方块 □。

### 根因分析

经全代码库扫描，确认 3 类 tofu 来源：

**类型 A — Emoji 用在不支持 emoji 的字体**：

| 位置 | 文本 | 字体 | 问题 |
|------|------|------|------|
| MainController 右键菜单 | `🖼` `📁` `▶` `⚡` `🔍` `🗑` | 默认 L&F | JMenuItem 无字体覆盖 |
| ToastNotification | `✕` | Microsoft YaHei | U+2715 不在雅黑中 |
| VideoParamPanel | `✕` | Dialog | U+2715 不在 Dialog 中 |

**类型 B — 非 emoji 字符用了 Segoe UI Emoji**：

| 位置 | 字符 | 问题 |
|------|------|------|
| FfmpegPlayerPanel | `↺` (U+21BA) | 箭头符号，不在 Segoe UI Emoji 中 → 永远 tofu |

**类型 C — 特殊符号在微软雅黑中缺失**：

| 位置 | 字符 | 问题 |
|------|------|------|
| StatusBar | `◉` `★` `✖` | 微软雅黑不含这些装饰符号 |

### 修复方案：用 FlatSVGIcon 或纯文本替代

| # | 文件 | 改动 | 说明 |
|---|------|------|------|
| 1 | `controller/MainController.java` | 6 处 | 右键菜单 JMenuItem 用 FlatSVGIcon 替代 emoji |
| 2 | `view/StatusBar.java` | 3 处 | ◉/★/✖ → 统一 ●（靠 ThemeUtil 颜色区分状态） |
| 3 | `view/ToastNotification.java` | 1 处 | ✕ → ×（U+00D7，微软雅黑支持） |
| 4 | `view/VideoParamPanel.java` | 1 处 | 删除按钮 ✕ → × |
| 5 | `view/FfmpegPlayerPanel.java` | 1 处 | ↺ → "重试" |

### 编译验证

`mvn compile` BUILD SUCCESS · 54 源文件 · 0 错误

---

## 2026-07-19 · Phase 12-14：压缩大小守卫 + 豆腐块残留修复 + UI 条件显示

### 提交信息

```
fix(service,view,controller): 压缩大小守卫 + 豆腐块修复 + UI条件显示改进
```

**提交哈希**: `e1fa44b` · **分支**: `feat/ui-bluerhyme`

### Phase 12: 压缩体积增大修复

**现象**：相同格式的图片/视频压缩后反而变大（如 JPEG→JPEG 压缩后更大）

**根因**：
- 图片：Thumbnailator `outputQuality(0.8)` 是绝对质量目标，非"压缩率"目标。输入若已以 quality=60 保存过，80 重编码必然更大
- 视频：CRF 23 + `-preset medium` 是恒定质量编码，输入若已是 CRF 28+ 或 HEVC 编码，重编码更大
- 两者均无压缩后大小对比逻辑

**修复**：

| # | 文件 | 改动 | 说明 |
|---|------|------|------|
| 1 | `model/CompressResult.java` | +16行 | 新增 `warning` 字段 + `addWarning()`/`hasWarning()` 方法 |
| 2 | `service/CompressService.java` | +23行 | 图片压缩后大小守卫：输出 > 输入时删除输出、保留原文件、添加 warning |
| 3 | `service/VideoCompressService.java` | +11行 | 视频压缩后大小守卫：输出 >= 输入时添加 warning |
| 4 | `controller/VideoController.java` | +5行 | 调用 `isEffectivelyNoOp()` 检查，防止无效压缩参数 |

**关键设计决策**：
- 图片：输出更大时直接用原文件替换输出（用户不会得到更大的文件）
- 视频：仅 warning 不删除输出（视频重编码成本高，用户可能接受"画质提升但体积增大"）
- `isEffectivelyNoOp()` 在单文件压缩入口检查，防止用户提交无意义的压缩任务

### Phase 13: 豆腐块残留修复（第二轮）

**现象**：视频参数面板分组标题、主题按钮、缩放按钮等仍有 emoji 豆腐块

**新增 HIGH 风险位置**（第一轮遗留）：

| 位置 | 字符 | 代码点 | 问题 |
|------|------|--------|------|
| VideoParamPanel:137 | 🎯 | U+1F3AF | 分组标题"画质预设"，FONT_TITLE (Microsoft YaHei) 无此字形 |
| VideoParamPanel:207 | 📂 | U+1F4C2 | 分组标题"输出设置"，同上 |
| MainFrame:633 | 🎨 | U+1F3A8 | 主题按钮文字前缀，FONT_SMALL 无此字形 |
| PreviewPanel:114 | ⊡ | U+22A1 | 数学运算符，Microsoft YaHei 无此字形 |
| VideoController:367,426-427 | ✓/✗ | U+2713/U+2717 | Dingbats 传递到 statusLabel (FONT_SMALL) |

**修复**：

| # | 文件 | 改动 | 说明 |
|---|------|------|------|
| 1 | `view/VideoParamPanel.java` | 3处 | 🎯📂→纯文字、deleteBtn ×→FlatSVGIcon |
| 2 | `view/MainFrame.java` | 1处 | 主题按钮 🎨→移除（已有 palette.svg） |
| 3 | `view/PreviewPanel.java` | 1处 | ⊡→`[ ]` 纯文字 |
| 4 | `controller/VideoController.java` | 3处 | ✓→`[OK]`、✗→`[FAIL]` |

### Phase 14: UI 条件显示改进

**改进项**：

| 功能 | 原来 | 改为 |
|------|------|------|
| 视频自定义文件名 | 输入框始终可见 | 复选框"自定义输出文件名"→勾选后显示输入框 |
| 覆盖设置 | 视频默认勾选（`setSelected(true)`） | 默认不勾选 |
| 覆盖配置模型 | `VideoCompressConfig.overwrite = true` | `overwrite = false` |
| 视频裁剪 | 字段始终可见 | 复选框"启用视频裁剪"→勾选后显示时间字段 |

**涉及文件**：
- `view/VideoParamPanel.java` — 三个条件显示改进（+35行）
- `model/VideoCompressConfig.java` — overwrite 默认 false

### 编译验证

`mvn compile` BUILD SUCCESS · 54 源文件 · 0 错误

### 本轮总变更

8 文件 · +107 行 · -16 行

---

## 2026-07-20 · GIF 颜色缩减压缩 + 模式切换分段控件 + 使用手册

**分支**: `feat/ui-bluerhyme`

### 提交清单

| 提交 | 功能 | 涉及文件 |
|------|------|---------|
| `89fbb86` | feat(service,view): GIF 颜色缩减压缩 | CompressConfig, ImageCompressUtil, ParamPanel, ImageController |
| `1e2d822` | style(view): 模式切换改为双段分段控件 | MainFrame, MainController |
| `cc34613` | docs(project): 添加项目使用手册 | 使用手册.md(新) |

### GIF 颜色缩减压缩

**需求**：GIF 压缩的核心手段是减少颜色数，但当前 GIF 走通用压缩管线无专属优化。

**方案**：
- CompressConfig 新增 `gifMaxColors` 字段（2-256，默认 256 不限制）
- ParamPanel 高级设置 Tab 末尾添加「GIF 颜色优化」滑块，**仅选中 GIF 文件时显示**，其他格式隐藏
- ImageController `onFileSelected()` 检测文件扩展名 → `showGifControls("gif".equals(ext))`
- ImageCompressUtil 新增 `compressGifWithColorLimit()`：通道级色阶量化（levels = ∛maxColors, step = 256/levels）→ TYPE_BYTE_INDEXED → ImageIO GIF writer

**涉及文件**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `model/CompressConfig.java` | +8行 | gifMaxColors 字段 + getter/setter |
| `view/ParamPanel.java` | +40行 | GIF 颜色滑块 + showGifControls()/getGifMaxColors() |
| `controller/ImageController.java` | +5行 | 选中 GIF 时显示控件 + buildConfig 读取 |
| `service/ImageCompressUtil.java` | +70行 | compressGifWithColorLimit() + quantizeChannel() |

### 模式切换双段分段控件

**需求**：原模式切换是单按钮纯文字 JToggleButton（"图片"/"视频"），用户首次使用时很难注意到可以切换模式。

**方案**：将单个 JToggleButton 替换为双段分段控件（参考 PreviewPanel 已有模式），两个按钮始终可见：

```
┌──────────────┬──────────────┐
│ 🖼 图片       │ 🎬 视频       │
│ (选中高亮)    │ (未选中灰色)   │
└──────────────┴──────────────┘
```

- 每段含 FlatSVGIcon（image.svg / film.svg）+ 文字
- ButtonGroup 互斥，选中段 BG_CARD 凸起 + PRIMARY 色
- 自绘背景和文字（FlatLaf ButtonUI 在 opaque=false 时不渲染文字）
- 复用 PreviewPanel.createSegToggle() 的绘制模式

**涉及文件**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `view/MainFrame.java` | +124/-42 | 字段替换 + createModeSegmentedControl() + createModeSegToggle() + 更新 switchCompressMode/isVideoMode/getter |
| `controller/MainController.java` | +8/-4 | 事件绑定从 1 个改为 2 个按钮 |

### 使用手册

新增 [使用手册.md](使用手册.md)，14 章全覆盖：
1. 简介与技术栈
2. 安装环境要求（JDK/FFmpeg/VLC）
3. 主界面布局图
4. 快速上手（图片/视频各 3 步）
5. 工具栏详解
6. 文件管理（导入/列表/搜索/右键菜单）
7. 图片压缩参数逐一详解（质量/缩放/格式/高级/GIF）
8. 视频压缩参数逐一详解（CRF/分辨率/帧率/音频/格式/裁剪/批量变体）
9. 批量处理与容错
10. 主题与外观（13 套）
11. 菜单栏
12. 快捷键参考（15 个）
13. 设置持久化
14. 常见问题 FAQ

### 编译验证

`mvn compile` BUILD SUCCESS · 54 源文件 · 0 错误

---

## 2026-07-21 · 7 项可用性改进（用户手册评审反馈修复）

**分支**: `feat/ui-bluerhyme`

### 背景

使用手册评审反馈了多项可用性问题。本轮集中修复 7 项 P0/P1 级别问题，核心目标：**让用户无需阅读手册即可直觉操作**。

### 提交清单

| 提交 | 功能 | 涉及文件 |
|------|------|---------|
| `2e971c7` | fix(view,controller): 7项可用性改进 | 7 个文件 |

### 详细改动

#### P0-1: 质量滑块标签 + 标注

**问题**：「压缩质量」歧义 — 用户不知道高质量=图片清晰还是压缩率高。

**方案**：标签改为「画质清晰度」+ 滑块两端加「体积更小 ←」「→ 更清晰」标注。

| 文件 | 改动 | 说明 |
|------|------|------|
| `view/ParamPanel.java` | +18/-4 | qualityWrapper 面板 + 两端 JLabel + 标签文本更改 |
| `view/VideoParamPanel.java` | +1/-1 | 标签「视频画质」→「视频清晰度」 |

#### P0-2: 参数标签澄清

**问题**：「百分比」「最大颜色数」无上下文，用户不知道含义。

**方案**：「百分比」→「缩放比例 (%)」+ tooltip，「最大颜色数」→「最大颜色数 (GIF专用)」。

#### P0-3: 删除工具栏重复"开始压缩"按钮

**问题**：共 3 个「开始压缩」按钮（工具栏 + ParamPanel + VideoParamPanel），用户困惑。

**方案**：删除工具栏 `compressBtn`，仅保留参数面板内按钮。

| 文件 | 改动 | 说明 |
|------|------|------|
| `view/MainFrame.java` | -5 | 字段 + 创建代码 + getter 删除 |
| `controller/MainController.java` | -2 | 事件绑定 + setEnabled 删除 |
| `controller/ImageController.java` | -1 | getCompressBtn().setEnabled() 删除 |
| `controller/VideoController.java` | -1 | 同上 |

#### P0-4: 修复模式切换清空图片列表 Bug

**根因**：`onModeToggle(false)` 调用 `fileListPanel.clearAllFiles()` 但未保存图片列表。

**方案**：MainController 新增 `savedImageFileList` 字段，切视频前保存 → 切回图片时恢复。

#### P1-5: 输出格式 ? 说明按钮

**方案**：输出格式 JComboBox 右侧添加 `?` 按钮，点击弹格式对比说明。

| 文件 | 改动 | 说明 |
|------|------|------|
| `view/ParamPanel.java` | +20/-2 | formatPanel + 图片格式说明弹窗 |
| `view/VideoParamPanel.java` | +20/-2 | 视频格式说明弹窗 |

#### P1-6: 图片预设扩展为 4 个语义化预设

**问题**：图片仅「标准/高效/高清」3 个无语义预设。

**方案**：「网络分享(60)/日常存档(80)/摄影作品(95)/极限压缩(30)」。

#### P1-7: 导入文件按日期倒序

**方案**：导入后按 `lastModified()` 降序排序，最新的在前。

| 文件 | 改动 | 说明 |
|------|------|------|
| `controller/ImageController.java` | +18 | addFilesToList 内排序 + getLastModifiedSafe() |
| `controller/VideoController.java` | +9 | importVideoFiles 内排序 |

#### P1-8: 预览区按钮替换为操作按钮

**问题**：缩放按钮「⊡/1:1」辨识度低且功能无用。

**方案**：替换为「打开原文件」+「打开输出目录」操作按钮（FlatSVGIcon + 主色边框）。

| 文件 | 改动 | 说明 |
|------|------|------|
| `view/PreviewPanel.java` | +52/-23 | 缩放按钮 → 操作按钮 + setter 回调 |
| `controller/MainController.java` | +36 | Desktop.open() 回调绑定 |
| `controller/ImageController.java` | +9 | getCurrentOutputDir() 方法 |

### 编译验证

`mvn clean compile` BUILD SUCCESS · 54 源文件 · 0 错误 0 警告

---

## 2026-07-21 · 预估输出大小 + 缩放实时预览 + 视频目标大小

**分支**: `feat/ui-bluerhyme`

### 提交清单

| 提交 | 功能 | 涉及文件 |
|------|------|---------|
| `89eea56` | feat(view,controller): 预估输出大小 + 缩放实时预览 | 5 个文件 |
| `45c72c3` | feat(service,model,view): 视频目标大小 — 码率模式 | 4 个文件 |

### P2-8: 预估输出大小显示

**需求**：用户调整参数后不知道输出文件大致多大。

**方案**：
- **图片**：质量滑块下方显示预估大小。公式 = `原始KB × (quality/100) × formatFactor`
  - Format factor: JPEG=0.30, PNG=1.00, BMP=3.00, WebP=0.25
  - 选中文件后显示，未选中时隐藏
- **视频**：CRF 滑块下方显示预估范围。查表法：quality 90+ → 70-100%, 70-90 → 50-80%, 50-70 → 30-55%, 30-50 → 20-40%, 15-30 → 10-25%, <15 → 5-15%

**涉及文件**：
| 文件 | 改动 | 说明 |
|------|------|------|
| `view/ParamPanel.java` | +20 | estimatedSizeLabel + setEstimatedSize/hideEstimatedSize |
| `view/VideoParamPanel.java` | +14 | 同上 |
| `controller/ImageController.java` | +37 | selectedOriginalSize + updateEstimatedImageSize() + formatFileSize() |
| `controller/VideoController.java` | +55 | selectedVideoOriginalSize + updateEstimatedVideoSize() + formatVideoSize() |

### P2-9: 缩放比例实时预览

**需求**：修改缩放比例后预览图应实时刷新。

**方案**：
- ImageUtil 新增 `generateEffectPreview(..., scalePercent)` 重载方法
- scaleModeCombo/scalePercentSpinner 变更 → 200ms 防抖 → refreshEffectPreview()
- 仅在「按百分比」模式下应用缩放比例

**涉及文件**：
| 文件 | 改动 | 说明 |
|------|------|------|
| `util/ImageUtil.java` | +18 | 新重载方法支持 scalePercent |
| `controller/ImageController.java` | +12 | 监听器绑定 + refresh 传入 scalePercent |
| `view/ParamPanel.java` | +1 | getScalePercentSpinner() |

### P0-5: 视频目标文件大小自动调节（码率模式）

**需求**：用户可以指定目标文件大小（MB），软件自动计算最优码率。

**方案**：
- VideoCompressConfig 新增 `RateControlMode { CRF, TARGET_SIZE }` + `targetSizeMB` + `targetBitrate`
- VideoCompressService 计算：`targetBitrate(kbps) = (targetMB × 8 × 1024 / duration) - 128`
- VideoCompressUtil：TARGET_SIZE 模式用 `-b:v`/`-maxrate`/`-bufsize` 替代 `-crf`
- UI：VideoParamPanel 输出设置组新增「编码模式」下拉框 + 目标 MB 输入框

> 注：图片目标大小二分搜索已在 `CompressService.compressWithTargetSize()` 中实现（bisect 8 轮 iterative 搜索 quality 值）。

**涉及文件**：
| 文件 | 改动 | 说明 |
|------|------|------|
| `model/VideoCompressConfig.java` | +28 | RateControlMode 枚举 + targetSizeMB + targetBitrate |
| `service/VideoCompressService.java` | +7 | 计算目标码率 |
| `service/VideoCompressUtil.java` | +10 | bitrate 模式命令构建 |
| `view/VideoParamPanel.java` | +35 | 编码模式 UI |

### 编译验证

`mvn clean compile` BUILD SUCCESS · 54 源文件 · 0 错误 0 警告

---

## 2026-07-21 · 4 项用户反馈 Bug 修复（第二轮）

### Bug 修复清单

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | 信息面板不能滚动 | `infoCardPanel` 直接加 Tab 无 JScrollPane | 包裹 JScrollPane（透明 + 无边框） |
| 2 | 格式 ? 弹窗按钮不可见 | Button.background (#FFF) ≈ Panel.background (#F1F5F9) | ThemeUtil.showStyledMessageDialog() 临时覆盖按钮色 |
| 3 | 缩放模式效果不明显 | 按最大尺寸预览不反映缩放 + 对比无尺寸标注 + 无 tooltip | 计算等效 scalePercent + 对比标签加 W×H + tooltip + 输出尺寸预览 |
| 4 | 目标大小不趋近 | bestQuality 初始化为滑块值，异常兜底失效 | 初始值 0 + fallback + 浅拷贝防竞态 + unreachable warning |

### Bug 修复详情

#### Bug 1: 信息面板滚动

**现象**：图片信息/视频信息 Tab 内容超出面板时被裁剪，只能拖拽窗口边缘放大。

**根因**：`PreviewPanel.java:166` 和 `VideoPreviewPanel.java:117` 中 `infoCardPanel` 直接 `addTab` 到 `JTabbedPane`，无滚动容器。参数面板（ParamPanel/VideoParamPanel）在 MainFrame 中已正确包裹了 JScrollPane。

**修复**：
```java
JScrollPane infoScroll = new JScrollPane(infoCardPanel);
infoScroll.setOpaque(false);
infoScroll.getViewport().setOpaque(false);
infoScroll.setBorder(null);
infoScroll.setHorizontalScrollBarPolicy(JScrollPane.HORIZONTAL_SCROLLBAR_NEVER);
infoScroll.getVerticalScrollBar().setUnitIncrement(16);
tabbedPane.addTab("图片信息", icon, infoScroll);
```

| 文件 | 改动 |
|------|------|
| `view/PreviewPanel.java` | +7 (JScrollPane 包裹 + import) |
| `view/VideoPreviewPanel.java` | +7 (同上) |

---

#### Bug 2: 格式弹窗按钮不可见

**现象**：点击格式 `?` 按钮弹出 JOptionPane，底部「确定」按钮融入背景，仅鼠标悬停时出现浅蓝高亮才可见。

**根因**：全局 `Button.background = BG_CARD (#FFFFFF)` 与 `Panel.background = BG_WINDOW (#F1F5F9)` 色差极小（ΔL≈1），FlatLaf 浅色边框同样不可见。

**修复**：ThemeUtil 新增 `showStyledMessageDialog()` 工具方法，弹窗前临时覆盖 OptionPane 按钮为 PRIMARY 背景 + 白色文字，弹窗后 finally 恢复。

| 文件 | 改动 |
|------|------|
| `util/ThemeUtil.java` | +22 (新方法 + import) |
| `view/ParamPanel.java` | -3/+3 (替换 JOptionPane 调用) |
| `view/VideoParamPanel.java` | -2/+2 (同上) |

---

#### Bug 3: 缩放模式效果可视化

**3a. 按最大尺寸预览不反映缩放**

`ImageController.refreshEffectPreview()` 中仅 scaleModeIndex==1（按百分比）时传入实际 scalePercent，mode==2（按最大尺寸）时硬编码 100。新增 `computeEffectScalePercent()` 方法：mode=2 时根据原图宽高与 1920×1080 的比值计算等效百分比。

**3b. 对比视图无尺寸标注**

`PreviewCanvas.drawComparison()` 的标签仅显示「原图」「效果」，改为显示「原图 4000×3000」「效果 2000×1500」含像素尺寸。

**3c. 缩放模式无解释 + 无输出尺寸预览**

- `scaleModeCombo` 添加 tooltip：「不缩放：保持原分辨率 | 按百分比：宽高等比缩放 | 按最大尺寸：限制宽高 ≤ 1920×1080」
- ParamPanel 新增 `outputDimLabel`，实时显示预计输出尺寸（如「输出尺寸：1920 × 1080（50%）」）
- `updateOutputDimLabel()` 在选中文件、切换缩放模式、修改百分比时触发

| 文件 | 改动 | 说明 |
|------|------|------|
| `controller/ImageController.java` | +55 | computeEffectScalePercent + updateOutputDimLabel + 监听 |
| `view/PreviewCanvas.java` | +2 | 对比标签含 W×H 尺寸 |
| `view/ParamPanel.java` | +20 | tooltip + outputDimLabel 字段 + setter/hide 方法 |

---

#### Bug 4: 图片目标大小二分搜索不收敛

**现象**：指定目标大小 500KB，实际输出远小于目标（如 200KB）。

**根因**：`CompressService.compressWithTargetSize()` 中 `bestQuality = config.getQuality()`（用户滑块值，如 80）。当 quality=5 试压缩异常失败时，`found` 保持 false，`bestQuality` 仍为 80。二分搜索条件 `mid > bestQuality` 跳过所有低于 80 的 mid。

**修复**：
1. `bestQuality = 0`（替代滑块值）→ 保证任意有效 quality 都能被记录
2. `!found` 时 fallback 到 `config.getQuality()`
3. 最终压缩前浅拷贝 config（`buildTrialConfig`）防止批量压缩竞态
4. quality≥100 仍不达目标时 `result.addWarning()` 提示用户

| 文件 | 改动 |
|------|------|
| `service/CompressService.java` | ~40 行重写 |

### 编译验证

`mvn compile` BUILD SUCCESS · 54 源文件 · 0 错误

### 涉及文件汇总

| 文件 | 改动类型 |
|------|---------|
| `view/PreviewPanel.java` | JScrollPane 包裹信息 Tab |
| `view/VideoPreviewPanel.java` | JScrollPane 包裹信息 Tab |
| `util/ThemeUtil.java` | 新增 showStyledMessageDialog() |
| `view/ParamPanel.java` | 替换弹窗调用 + tooltip + outputDimLabel |
| `view/VideoParamPanel.java` | 替换弹窗调用 |
| `controller/ImageController.java` | scalePercent 计算 + 输出尺寸标签逻辑 |
| `view/PreviewCanvas.java` | 对比视图尺寸标签 |
| `service/CompressService.java` | 二分搜索算法修复 |

---

## 2026-07-21 · 缩放参数区域 UX 优化

### 用户反馈
1. 参数面板「缩放模式/缩放比例」功能不直观，用户不理解缩放到底缩的是什么
2. 缩放比例下方 outputDimLabel 出现文字重影（两层字体重叠）
3. 预览对比模式图片左上角 W×H 标签没必要、碍眼

### 修复内容

#### Fix 1：outputDimLabel 文字重影
- **根因**：JLabel 默认 `opaque=false`，父面板也是透明的。`setText()` 改变文字时不绘制背景，旧文字像素残留在下方
- **解决**：`outputDimLabel.setOpaque(true)` + `setDynamicBackground()` 匹配卡片背景色

#### Fix 2：去掉对比视图 W×H 标签
- 直接删除 `PreviewCanvas.drawComparison()` 中两处 `drawComparisonLabel()` 调用及方法定义

#### Fix 3：缩放模式下方新增通俗说明
- 在 `ParamPanel` 新增 `scaleHintLabel`，位于 `outputDimLabel` 和"输出格式"之间
- 三种模式各有对应说明文字：
  - 不缩放 → "输出图片保持原始分辨率，像素尺寸不变，仅通过压缩质量减小文件体积"
  - 按百分比 → "按百分比缩小输出图片的宽高，像素总数减少 → 文件自然变小"
  - 按最大尺寸 → "限制输出图片不超过 1920×1080，超出自动等比缩小；未超出则保持原尺寸"
- 切换模式/选中文件时动态更新；取消选中时隐藏

### 提交信息
`feat(view): 缩放参数区域交互优化`
`3a75edf`

### 涉及文件
| 文件 | 改动 |
|------|------|
| `view/ParamPanel.java` | outputDimLabel 不透明 + 新增 scaleHintLabel + API |
| `view/PreviewCanvas.java` | 删除 drawComparisonLabel 方法及调用 |
| `controller/ImageController.java` | SCALE_HINTS 常量 + 联动更新 hint |

### 编译验证
`mvn compile` BUILD SUCCESS

---

## 2026-07-21 · 缩放模式旁添加 ? 说明按钮

### 用户反馈
缩放参数旁边的说明文字不够醒目，希望在「缩放模式」旁加一个 `?` 按钮，点击弹出详细说明。

### 修复
参照 `formatInfoBtn` 样式，将 `scaleModeCombo` 包裹进 `JPanel(BorderLayout)`，右侧放 `?` 按钮，点击调用 `ThemeUtil.showStyledMessageDialog()` 展示缩放功能说明。

### 提交信息
`feat(view): 缩放模式旁添加 ? 说明按钮`
`1a7cef1`

### 涉及文件
| 文件 | 改动 |
|------|------|
| `view/ParamPanel.java` | scaleModeCombo 改为 panel 包裹 + ? 按钮 |

---

## 2026-07-22 · 文字重影第二轮修复

### 用户反馈
第一轮用 `setOpaque(true)` 修复 outputDimLabel 重影无效，重影仍然存在。

### 根因
JLabel 的 `setOpaque(true)` 在 FlatLaf 主题下行为不确定——FlatLaf 可能覆盖了 opaque 属性或绘制行为。JPanel 则是 Swing 原生组件，`opaque=true` 保证填充背景色。

### 解决
将 `outputDimLabel` 和 `scaleHintLabel` 分别包裹在不透明 JPanel（dimWrapper / hintWrapper）中。JPanel 以自己的背景色填充整个区域后再渲染子组件，物理上不可能残留旧文字像素。API 方法改为同时控制 label 和 wrapper 的显隐。

### 提交信息
`fix(view): 输出尺寸/说明标签改为 JPanel 包裹以彻底消除文字重影`
`bbbdd8d`

### 涉及文件
| 文件 | 改动 |
|------|------|
| `view/ParamPanel.java` | label → wrapper 包裹，API 联动 |
--- 

## 2026-07-22 · 视频裁剪 UI 修复

### 现象
输入起始 3、时长 17，用户期望 14 秒（3→17 区间），实际输出 17 秒。

### 根因
第二个输入框标注为「时长」，用户理解为「结束时间」。FFmpeg `-t 17` 解释为持续 17 秒。

### 修复
将字段改为「结束」（endTimeField），内部计算 duration = endTime - startTime 再写入 config。

### 提交
`fix(view): 视频裁剪字段「时长」改为「结束」— 消除起止区间歧义`
`0944963`

---

## 2026-07-22 · 新增 5 套撞色主题（浅深双模共 10 种配色）

### 背景
用户提供《NCHU Compressor 主题设计描述规范》，要求基于便携版新增撞色主题。
规范定义 5 套主题，每套含浅色/深色双模，遵循 60-30-10 法则、WCAG 对比度标准。

### 实施
- `ThemePalette.java`：新增 `darken/lighten/blendOnWhite/blendOnBg` 4 个颜色工具方法 + 10 个工厂方法（~230 行）
- `Theme.java`：新增 10 个枚举值，主题总数从 13 增至 23
- 无需改动 ThemeUtil / ThemePopup / MainFrame / AppConfig，现有基础设施自动适配

### 5 套主题
| # | 主题 | 撞色 | 气质 |
|---|------|------|------|
| 1 | 紫黑电竞 | #A855F7 + #0B0C10 | 专业神秘、未来感 |
| 2 | 绿米自然 | #73AE52 + #FBF1D7 | 温润自然、久看不累 |
| 3 | 雾蓝冷感 | #0EA5E9 + #D1D5DB | 冷静专业、工程师 |
| 4 | 嫩绿蓝清新 | #05A5FA + #D3FFAF | 年轻活力、清新 |
| 5 | 浅粉红暖意 | #E72D48 + #F1DDDF | 温柔有力、差异化 |

### 提交
`feat(model): 新增 5 套撞色主题（浅深双模共 10 种配色方案）`
`a163dac`

---

## 2026-07-22 · 按钮边框主题色 + 分段控件选中态增强

### 背景
用户反馈按钮边框使用中性灰色（`BORDER`）与主题强调色脱节，模式切换（图片/视频）和预览切换（原图/效果/对比）选中态不够突出。

### 实施
- `ThemeUtil.java`：全局 `Button.borderColor` 从 `BORDER` 改为 `PRIMARY`，所有 FlatLaf 默认按钮自动获得主题色边框
- `MainFrame.java`：
  - 次级按钮（导入文件/导入文件夹）默认边框从 `BORDER` → `PRIMARY`，hover → `PRIMARY_DEEP`
  - 模式切换分段控件选中态从 `BG_CARD`+`PRIMARY`文字 → `PRIMARY`填充+白色文字
- `PreviewPanel.java`：预览分段切换（原图/效果/对比）选中态同步改为 PRIMARY 填充+白字
- `ParamPanel.java`：取消按钮移除 `setBorderPainted(false)`，添加 PRIMARY 线边框
- `VideoParamPanel.java`：同上

### 设计考量
- 60-30-10 规则：1px 细边框仅占极小面积，不突破 10% 强调色上限
- 分段控件选中态统一为「实心彩色药丸」，与预设按钮（网络分享/日常存档）选中语言一致

### 提交
`style(view): 按钮边框使用主题强调色 + 分段控件选中态 PRIMARY 填充`
`19a5e07`

---

## 2026-07-22 · 输入控件边框主题色

文件搜索框、下拉框、数值调节器边框从 BORDER 改为 PRIMARY，与按钮边框统一。
- `TextComponent.borderColor` → PRIMARY（覆盖所有 JTextField/JSpinner）
- `ComboBox.borderColor` → PRIMARY（覆盖所有 JComboBox）

### 提交
`style(util): 输入控件边框改为主题强调色 — TextComponent + ComboBox borderColor PRIMARY`
`ea4da79`

### Bug 修复
- **现象**：设置 `TextComponent.borderColor` / `ComboBox.borderColor` 后输入框边框无变化
- **原因**：FlatLaf 3.2.5 不识别这两个键，边框色实际从 `Component.borderColor` 继承
- **解决**：改为 `Component.borderColor = PRIMARY`，覆盖所有文本/下拉/微调控件
- **教训**：UIManager 键名必须以 FlatLaf 源码中的 `.properties` 定义为准，不能猜测

### 提交
`fix(util): 修复输入控件边框 — TextComponent/ComboBox.borderColor 改为 Component.borderColor`
`5c31e15`

### Bug 修复（第二次）
- **现象**：换肤后输入框/下拉框边框颜色不更新，首次显示的是上个主题的颜色
- **原因**：`Component.borderColor` 是 FlatLaf 内部派生值（`shade(@background, 20%)`），`updateUI()` 时会从背景色重新推导并覆盖我们 set 的值
- **解决**：完全放弃 UIManager 全局键方案，改为 `createDynamicLineBorder()` 工厂方法——`paintBorder()` 时实时读取 `ThemeUtil.PRIMARY`，无需缓存刷新
- **教训**：FlatLaf 的派生键（`$Component.xxx` / `shade()` / `tint()` 语法）在 `updateUI()` 时会被重新计算，不能用 `UIManager.put()` 永久覆盖

### 提交
`fix(view): 输入控件边框改用动态 LineBorder — 实时读取 PRIMARY，解决 FlatLaf 换肤后边框不更新`
`3ef1cbd`

---

## 2026-07-22 · 新功能：压缩完成自动定位输出文件

### 功能描述
压缩任务完成后，点击"打开输出目录"按钮直接在 Windows 资源管理器中高亮选中本次生成的文件（首个成功文件），无需手动在目录中查找。

### 实现方案
- **FileUtil.openFileInExplorer()**：调用 `explorer /select, "<文件绝对路径>"` 定位文件
- **ResultDialog**：按钮改为定位首个成功文件；新增"完成后自动定位文件"checkbox，偏好持久化到 AppConfig
- **ImageController**：单文件压缩成功后自动触发定位（如用户启用）

### 涉及文件
| 文件 | 改动 |
|------|------|
| `util/FileUtil.java` | 新增 `openFileInExplorer(String)` |
| `view/ResultDialog.java` | 按钮改为定位文件 + 新增自动定位 checkbox |
| `model/AppConfig.java` | 新增 `autoRevealOutput` 字段 |
| `controller/ImageController.java` | 单文件压缩成功后自动定位 |
| `controller/VideoController.java` | ResultDialog.show() 签名适配 |

### 提交
`feat(view,util): 压缩完成自动在资源管理器中定位输出文件`
`95868e6`

---

## 2026-07-22 · 新功能：鼠标悬停极速大图预览

### 功能描述
鼠标悬停文件列表任意行 0.5 秒后，在光标旁弹出 200×200 半透明预览窗口。图片显示缩略图，视频显示首帧快照。鼠标移开，预览窗口立即销毁。

### 实现方案
- **HoverPreviewPopup**：封装 JWindow（无焦点、置顶），主题色 1px 边框，图片等比例缩放
- **FfmpegRenderUtil**：`startSingleFrame(File, int, double seekSeconds)` 新重载，原方法委托新方法
- **FileListPanel**：
  - MouseMotionListener 监听行变化 → 重置 500ms Timer
  - 大缩略图缓存（200×200 key#200）在导入时预生成
  - 视频首帧通过 FFmpeg 异步提取，缓存后二次悬停即时显示
- **MainController**：窗口关闭时 dispose 弹窗资源

### 涉及文件
| 文件 | 改动 |
|------|------|
| `view/HoverPreviewPopup.java` | **新建** — JWindow 悬停预览弹窗 |
| `util/FfmpegRenderUtil.java` | startSingleFrame 新增 seekSeconds 重载 |
| `view/FileListPanel.java` | +MouseMotionListener +Timer +大缩略图缓存 +视频帧异步提取 |
| `controller/MainController.java` | 窗口关闭时释放弹窗 |

### 提交
`feat(view,util): 鼠标悬停极速大图预览 — 0.5s 延迟弹出 200×200 缩略图`
`1a3a44c`

---

## 2026-07-22 · 新功能：视频封面快照生成

### 功能描述
视频压缩参数面板新增"同时提取封面"选项，可指定截取时间点（默认第 5 秒）和输出格式（JPG/PNG）。压缩成功后自动在输出目录生成对应封面文件。

### 实现方案
- **CoverExtractUtil**：新建工具类，调用 `FfmpegRenderUtil.startSingleFrame()` → `readFrame()` → `rgbToImage()` → `ImageIO.write()`
- **VideoParamPanel**：新增封面控件行（checkbox + 时间输入 + JPG/PNG 下拉框）
- **VideoCompressConfig**：新增 `extractCover` / `coverSeekSeconds` / `coverFormat` 字段
- **VideoCompressService**：压缩成功后自动提取封面，非致命失败静默跳过

### 涉及文件
| 文件 | 改动 |
|------|------|
| `util/CoverExtractUtil.java` | **新建** — 封面提取工具类 |
| `model/VideoCompressConfig.java` | +3 字段 + getter/setter |
| `model/CompressResult.java` | +coverPath + getter/setter/hasCover |
| `model/AppConfig.java` | +3 持久化字段 |
| `view/VideoParamPanel.java` | +封面控件行 + buildConfig 集成 |
| `service/VideoCompressService.java` | +封面提取调用 |

### 提交
`feat(service,view,util): 视频封面快照生成`
`61c3d6e`

---

## 2026-07-22 · 新功能：文件名智能解析·批量重命名

### 功能描述
右键菜单新增"批量重命名"，支持 {date}/{counter}/{name}/{ext}/{text} Token 模板，实时预览冲突检测，确认后批量执行磁盘重命名。

### 实现方案
- **RenameRule**：Token 引擎 + 日期正则提取（yyyyMMdd/yyyy-MM-dd/yyyyMMdd_HHmmss）
- **RenameService**：preview() 生成预览条目 + 冲突检测；execute() 执行 File.renameTo()
- **BatchRenameDialog**：模板编辑器 + JTable 实时预览 + 状态颜色编码
- **FileInfo**：新增 setSourceFile/setFileName 接口方法

### 涉及文件
| 文件 | 改动 |
|------|------|
| `model/RenameRule.java` | **新建** — 重命名规则 Token 引擎 |
| `service/RenameService.java` | **新建** — 预览+执行逻辑 |
| `view/BatchRenameDialog.java` | **新建** — 重命名对话框 |
| `model/FileInfo.java` | +setSourceFile +setFileName |
| `controller/MainController.java` | +右键菜单项 + onBatchRename() |

### 提交
`feat(view,service,model): 文件名智能解析·批量重命名`
`d842077`

---

## 2026-07-22 · 新功能：懒人自动压缩模式（文件夹监控 + 系统托盘）

### 功能描述
设置源文件夹后自动监控新增文件，按预设参数自动压缩到输出目录。支持最小化到系统托盘后台运行，压缩完成推送气泡通知。

### 实现方案
- **FolderWatchService**：JDK WatchService + 文件写入完成检测（轮询大小，2 次稳定 = 完成）+ 去重
- **WatchController**：生命周期管理 + CompressService/VideoCompressService 调度
- **SystemTrayManager**：java.awt.SystemTray 集成，16x16 图标，右键菜单，气泡通知
- **WatchSetupDialog**：文件夹选择 + 质量滑块 + 启动/停止切换

### 涉及文件
| 文件 | 改动 |
|------|------|
| `model/FolderWatchConfig.java` | **新建** |
| `service/FolderWatchService.java` | **新建** |
| `controller/WatchController.java` | **新建** |
| `view/SystemTrayManager.java` | **新建** |
| `view/WatchSetupDialog.java` | **新建** |
| `model/AppConfig.java` | +watchConfig +minimizeToTray |
| `view/MainFrame.java` | +工具栏按钮 |
| `controller/MainController.java` | +托盘/监控逻辑 |

### 提交
`feat(service,controller,view): 懒人自动压缩模式`
`18437a4`

---

## 2026-07-22 — v2.5 五重大修复

### 问题 1: 批量重命名 UX 优化
- **现象**：用户点击"📝 批量重命名..."无反应、不知道有格式输入框
- **原因**：空列表时只有 Toast（易忽略）；默认模板 `{name}` 输出与原名相同，预览无变化；模板语法 `{date} {counter}` 对新手不友好
- **解决**：
  - 空列表时显示 `JOptionPane.showConfirmDialog` 询问"是否导入文件"
  - 添加 4 个预设模板按钮（日期+序号、自定义文本+序号、原名+序号、保留原名）
  - 默认模板改为 `{date}_{counter:3}`
  - 基础 Tab 增加批量重命名按钮
- **涉及文件**：`MainController.java`, `BatchRenameDialog.java`, `ParamPanel.java`

### 问题 2: 压缩完成后对比显示两张不同图片（用户确认 bug）
- **现象**：批量压缩完成后，[对比] 模式左右显示不同文件的图片
- **原因**：`onCompressComplete()` 只调 `showEffect()` 加载效果图，原图仍是压缩前最后手动选中的文件
- **解决**：背景线程同时加载第一成功结果的原图和效果图，统一调度到 EDT，确保对比模式下是同一文件
- **涉及文件**：`ImageController.java`

### 问题 3: 文件夹监控可发现性
- **现象**：用户不知文件夹监控功能如何使用
- **原因**：无工具栏状态指示、无首次使用引导、监控状态不可见
- **解决**：
  - 工具栏按钮监控中时文字变为"● 监控中"（绿色）
  - 对话框首次打开时顶部显示引导文字
  - 状态栏显示"📁 监控中: ..."
- **涉及文件**：`MainFrame.java`, `WatchSetupDialog.java`, `MainController.java`, `StatusBar.java`

### 问题 4: 图片信息命名
- **现象**：文件名不能包含分辨率、文件大小等图片元数据
- **解决**：
  - `CompressConfig.NamingRule` 新增 `INFO_BASED` 枚举值
  - `CompressService.buildOutputFileName()` 签名改为 `(ImageFileInfo, CompressConfig)`
  - 新增 `expandInfoPattern()` 支持 `{name} {width} {height} {size} {sizeKB} {format} {quality}` 令牌
  - ParamPanel 下拉框新增"基于图片信息（分辨率/大小等）"选项，默认模板 `{name}_{width}x{height}`
- **涉及文件**：`CompressConfig.java`, `CompressService.java`, `ParamPanel.java`, `ImageController.java`, `AppConfig.java`

### 问题 5: 逐文件压缩参数
- **现象**：不能为单个文件设置不同的压缩参数
- **解决**：
  - 新建 `PerFileCompressConfig` 模型（可空字段 = 使用全局）
  - `CompressConfig.applyOverride()` 生成合并配置
  - `ImageFileInfo` 新增 `perFileConfig` 字段
  - `BatchCompressService` 在压缩任务中应用 per-file effective config
  - 新建 `PerFileConfigDialog` 对话框
  - 右键菜单新增"⚙ 单独设置此文件参数…"
  - 文件列表中有 override 的文件显示 ⚙ 蓝标
- **涉及文件**：`PerFileCompressConfig.java`（新）, `PerFileConfigDialog.java`（新）, `CompressConfig.java`, `ImageFileInfo.java`, `BatchCompressService.java`, `MainController.java`, `FileListPanel.java`

### 提交
`feat(view,model,service,controller): v2.5 — 批量重命名UX优化 + 对比修复 + 文件夹监控 + 图片信息命名 + 逐文件参数`
`2fefc28`
- 18 个文件变更，+632 / -27 行
- 2 个新文件：PerFileCompressConfig, PerFileConfigDialog

### v2.5.1 追修 — 批量重命名后文件列表丢失
`fix(view,controller): v2.5.1 — 批量重命名后使用 addFiles 批量导入，避免逐文件 applyFilter 导致文件列表丢失`
`9d3fd86`
- **现象**：批量重命名后文件列表只显示两个
- **原因**：回调逐文件调 `addFile()` → 每次触发 `applyFilter()` → `listModel.clear()` + 重建 N 次，EDT 密集事件队列中中间态列表渲染导致部分文件丢失
- **解决**：`FileListPanel` 新增 `addFiles()` 批量方法，一次性追加 + 统一 `applyFilter` + `repaint/revalidate`
- **涉及文件**：`FileListPanel.java`, `MainController.java`

---

## 2026-07-23 — v2.5.2 三修复

### 问题 1: 批量压缩后文件列表丢失（只显示少数文件）

- **现象**：导入多张图片 → 批量压缩 → 文件列表只剩少数文件，其余的消失
- **原因**（双重根因）：
  1. `getImageFileList()` 从 `listModel`（过滤后）而非 `allFiles`（主数据源）读取文件列表。若搜索框残留文字，`listModel` 被过滤到仅匹配文件，压缩任务就只处理这部分
  2. `addFilesToList()` 逐文件调用 `addFile()`，每次触发 `applyFilter()` → `listModel.clear()` + 重建，与批量重命名 v2.5.1 同一模式
- **解决**：
  1. `FileListPanel` 新增 `getAllImageFiles()` 方法，直接从 `allFiles` 主数据源读取，不受搜索/排序过滤影响
  2. `onStartCompress()` 改用 `getAllImageFiles()` 获取压缩文件列表
  3. `addFilesToList()` 改用 `addFiles()` 批量导入
- **涉及文件**：`FileListPanel.java`, `ImageController.java`

### 问题 2: 逐文件参数在单文件压缩时不生效

- **现象**：右键"单独设置此文件参数"（质量 30）后，右键"单独压缩此文件"仍用全局滑块值
- **原因**：`ImageController.compressSingleFile()` 直接从 `buildCompressConfig()` 构建全局配置，完全未调用 `applyOverride()`。`BatchCompressService` 两处路径都正确应用了 override，但单文件路径遗漏
- **解决**：压缩前检查 `info.hasPerFileConfig()`，有则调用 `config.applyOverride()`
- **涉及文件**：`ImageController.java`

### 问题 3: 压缩后切换文件 → [对比] 显示两张不同图片（回归修复）

- **现象**：批量压缩 A/B/C → 预览显示 A 的 before/after → 点击 B → 开启对比 → 左边 B 的原图、右边 A 的压缩效果
- **原因**（三个子 Bug）：
  1. `onFileSelected()` 只更新原图槽位，**不清除 `canvas.effectImage`**（v2.5 的修复只在压缩完成后加载了匹配原图，但未处理用户切换文件后 `effectImage` 残留）
  2. 对比数据浮层（原始大小/压缩后/压缩率）选不同文件时不清除，显示旧数据
  3. `onCompressComplete()` 的 `break` 只预览第一个成功结果
- **解决**：
  1. `onFileSelected()` 切换文件时调用 `previewPanel.clearEffect()` + `clearComparison()`
  2. `PreviewCanvas` 新增 `clearEffectImage()`（仅清除效果图槽位，保留原图）
  3. `PreviewPanel` 新增 `clearEffect()` + `clearComparison()` 方法
- **涉及文件**：`PreviewCanvas.java`, `PreviewPanel.java`, `ImageController.java`

### 提交
`fix(view,controller): v2.5.2 — 修复批量压缩文件丢失 + 逐文件参数 + 对比错乱`
`3ac831e`
- 4 个文件变更，+55 / -4 行
- 无新文件

---

## 2026-07-23 — v2.5.3 双修复

### 问题 1: 对比预览仅第一个文件有效，切换文件后对比失效（v2.5.2 回归）

- **现象**：批量压缩 A/B/C → 正常预览 A 的 before/after → 点击 B → 再点对比 → 无效果图显示
- **原因**（回归）：
  1. v2.5.2 为防对比错乱，在 `onFileSelected()` 无条件调用了 `clearEffect()`，但 `CompressResult` 列表从未持久化（`onCompressComplete()` 中局部变量，方法结束即丢失）
  2. 用户选 B 后，A 的 effect 被清除，B 的压缩结果早已不可获取
- **解决**：
  1. `ImageController` 新增 `resultCache`（`Map<String, CompressResult>`），在 `onCompressComplete()` 中填充所有成功结果
  2. `onFileSelected()` 智能切换：查缓存命中 → 磁盘加载该文件的压缩输出 → `showEffect()` + `updateComparison()`；未命中 → `clearEffect()` + `clearComparison()`（保留 v2.5.2 防错乱）
  3. `onStartCompress()` 和 `clearPreview()` 清空缓存
  4. 新增 `updateEffectForCachedResult()` 辅助方法
- **涉及文件**：`ImageController.java`

### 问题 2: "输出尺寸"文字位置错误，与缩放控件重叠

- **现象**：选择文件后"输出尺寸：1920×1080（原尺寸）"文字显示在缩放百分比控件的同一行，视觉重叠
- **原因**：`ParamPanel.createBasicTab()` 中 `dimWrapper` 和 `hintWrapper` 两个全宽 GridBagLayout 行在 `panel.add()` 前缺少 `gbc.gridy = row`，导致 `gridy` 沿用上一行的值，与缩放控件放置在同行
- **解决**：两处各加 `gbc.gridy = row;`
- **涉及文件**：`ParamPanel.java`

### 提交
`fix(view,controller): v2.5.3 — 修复对比预览仅首文件有效 + 输出尺寸标签错位`
`a307bff`
- 4 个文件变更，+101 / -3 行（含进度文档）
- 无新文件

---

## 2026-07-23 — v2.5.4 批量重命名入口重构

### 背景
批量重命名有三处入口（基础 tab 代理按钮 + 高级 tab 独立按钮 + 右键菜单），冗余且位置不合理（参数面板应专注压缩配置，不应放置文件管理操作按钮）。

### 改动
- **删除基础 tab "批量操作"行**（`basicRenameBtn` 及布局代码）
- **删除高级 tab `batchRenameButton`** 独立按钮（字段、创建代码、getter、MainController wiring）
- **在"文件命名"下拉框中新增"批量重命名..."选项**（第 6 项），选中即弹出 `BatchRenameDialog`
- **新增 `Runnable` 回调注入**：`setOnBatchRenameAction(this::onBatchRename)` 替代原先的 `getBatchRenameButton().addActionListener()`
- **下拉框恢复逻辑**：选中 index 5 后触发动作，然后通过 `SwingUtilities.invokeLater` 恢复为上一个命名规则选项
- **右键菜单"批量重命名"保留不变**

### 设计要点
- 使用 `SwingUtilities.invokeLater` 避免在 `ItemListener` 中直接修改 combo 索引导致递归事件
- 通过 `Runnable` 回调保持 MVC 分层：View 层不直接依赖 Controller

### 涉及文件
- `view/ParamPanel.java` — 删除 2 个按钮、1 个 getter，新增回调字段 + setter + combo 选项 + ItemListener 逻辑
- `controller/MainController.java` — 删除 `addActionListener` wiring，改为 `setOnBatchRenameAction` 注入

### 提交
`refactor(view,controller): 批量重命名入口从按钮迁移到文件命名下拉框`
`082f533`
- 3 个文件变更，+26 / -46 行
- 无新文件

---

## 2026-07-23 — v2.5.5 修复批量重命名对话框不显示

### 现象
批量重命名多个文件时，只有前两个图片能重命名。

### 原因
**`BatchRenameDialog` 构造函数从未调用 `setVisible(true)`**，自 v2.5 初始提交（`d842077`）起即存在此缺陷。模态 `JDialog` 不会自动可见——构造函数执行完毕后对话框对象被 GC 回收，`executeRename()` 从未被用户触发。

### 解决
在 `BatchRenameDialog` 构造函数末尾（`refreshPreview()` 之后）添加 `setVisible(true);`

### 涉及文件
- `view/BatchRenameDialog.java` — 构造函数末尾 +3 行

### 提交
`fix(view): BatchRenameDialog 构造函数缺少 setVisible(true) 导致对话框从不显示`
`681c6d9`

---

## 2026-07-23 — v2.5.5 INFO_BASED 增加 {counter} 序号 + 清理 v2.5.4

### 背景
用户澄清"批量重命名"实际指**压缩输出文件的命名规则**（`NamingRule.INFO_BASED`），而非源文件重命名（`BatchRenameDialog`）。

v2.5.4 误将"批量重命名..."加入 `namingRuleCombo` 连接到 `BatchRenameDialog`，属于理解偏差。

### 根因
INFO_BASED 模式支持 `{name}` `{width}` 等占位符，但缺少 `{counter}` 序号。批量压缩时，CUSTOM 模式所有文件同名，依赖 `FileUtil.generateUniqueFilename()` 去重——在多线程并行压缩下存在 TOCTOU 竞态。

### 解决
1. **CompressConfig** — 新增 `batchIndex` 字段（1-based 序号）+ `shallowCopy()` 独立副本方法 + `applyOverride()` 保留拷贝
2. **CompressService.expandInfoPattern()** — 新增 `{counter}` 和 `{counter:N}` token 替换
3. **BatchCompressService** — 并行/串行路径均为每个文件调用 `config.shallowCopy()` 创建独立副本并设置 `batchIndex = index + 1`
4. **ParamPanel** — 恢复 `namingRuleCombo` 为 5 选项，删除 v2.5.4 的 `onBatchRenameAction` 回调 + `previousNamingIndex`
5. **MainController** — 删除 `setOnBatchRenameAction` wiring
6. **infoPatternField** — 默认值改为 `{name}_{counter:3}`

### 设计要点
- `shallowCopy()` 为每文件创建独立配置，解决并行 TOCTOU 竞态
- `{counter:N}` 必须先于 `{counter}` 替换（regex 优先），避免字面替换破坏格式参数
- `batchIndex=0` 兜底为 1，单文件压缩也正常输出

### 涉及文件
- `model/CompressConfig.java` — +38 行
- `service/CompressService.java` — +23 行
- `service/BatchCompressService.java` — 重构
- `view/ParamPanel.java` — -32 行（清理）
- `controller/MainController.java` — -2 行

### 提交
`feat(model,service): v2.5.5 — INFO_BASED 模式增加 {counter} 序号支持 + 清理 v2.5.4 残留`
`03a0460`
- 6 个文件，+78 / -38 行

---

## 2026-07-23 — v2.5.6 CUSTOM 模式自动序号兜底 + UI 文字层级优化

### 背景
用户反馈批量压缩时 CUSTOM 自定义文件名模式只输出 2 个文件（INFO_BASED 正常）。根因：`customNameField` 无 placeholder 提示，用户未输入 `{counter}` token → 所有文件同名 → `generateUniqueFilename` TOCTOU 竞态 → 只存活约 2 个。

同时用户反馈 UI 文字缺乏主次层级，部分标题不明显。

### 解决

**CUSTOM 自动序号兜底：**
1. **CompressService.buildOutputFileName() CUSTOM case** — 当 `batchIndex > 0` 且 `customName` 不含 `{counter}` 时，自动追加 `-{counter}`
2. **CompressService** — 提取 `expandCounterTokens()` 共用方法（DRY 重构），CUSTOM 和 INFO_BASED 共享
3. **ParamPanel** — `customNameField` 添加 placeholder 文本 `例: 旅游美景-{counter}`

**UI 文字层级优化：**
4. **ParamPanel** — 复用 VideoParamPanel 的 `addGroupHeader()` 模式，基础/高级 Tab 内新增分组标题：
   - 基础：🎯 画质设置 / 📐 尺寸与格式
   - 高级：📝 文件命名 / 📂 输出设置 / 🔧 高级选项
   - 分组标题使用 `FONT_TITLE` (Bold 15px) + 主题色 + 底部分隔线
5. **ParamPanel / PreviewPanel** — 面板标题统一为 `FONT_TITLE`
   - `"压 缩 参 数"`: `FONT_SMALL.deriveFont(BOLD)` → `FONT_TITLE`
   - `"预 览"`: `FONT_SMALL.deriveFont(BOLD)` → `FONT_TITLE`

### 设计决策
- 不在 `ThemePalette` 中新增 `FONT_LABEL` 常量，直接用 `FONT_TITLE` 减少改动面
- `addGroupHeader()` 直接复刻 VideoParamPanel 的同名方法，保持代码风格一致
- 自动追加序号是安全兜底：用户不输入语法也能得到正确结果

### 涉及文件
- `service/CompressService.java` — CUSTOM auto-counter + 方法提取
- `view/ParamPanel.java` — 5 个 group header + 标题统一 + placeholder
- `view/PreviewPanel.java` — 标题统一

### 提交
`style(view,service): CUSTOM 模式自动序号兜底 + UI 文字层级优化`
`41c7b95`
- 3 个文件，+75 / -12 行

### v2.5.6 追加：分组标题 emoji 改为 bullet（避免 Windows 乱码）

`41c7b95` 中 ParamPanel 分组标题使用了 emoji（🎯📐📝📂🔧），在部分 Windows 系统上可能显示为乱码方块。
改为 `●` (Unicode bullet) 保证跨平台可读性。涉及文件：`view/ParamPanel.java`（5 处替换）。

---

## 2026-07-23 — v2.5.7 修复应用启动崩溃 + WatchSetupDialog 空白

### 问题 1：启动崩溃（NoClassDefFoundError）

**现象**：应用启动即崩溃，主窗口从未出现。报错：
```
NoClassDefFoundError: com/github/weisj/jsvg/parser/SVGLoader
  at FlatSVGIcon.<clinit>
    → MainFrame.createSecondaryButton → initToolBar → <init> → 崩溃
```

**原因**：`flatlaf-extras:3.2.5` 的 `FlatSVGIcon` 类在加载 SVG 图标时需要 `jsvg` 库解析 SVG 文件。虽然 `fltlaf-extras` POM 声明了 `jsvg:1.2.0` 为 runtime 传递依赖，但 `exec-maven-plugin` 的自定义 `URLClassLoader` 未正确解析该传递依赖 → classpath 中缺少 `SVGLoader` → `FlatSVGIcon` 静态初始化失败 → `MainFrame` 构造函数中途崩溃。

**解决**：`pom.xml` 中显式添加 `com.github.weisj:jsvg:1.2.0` 依赖，强制 Maven 解析器将其加入 classpath。

### 问题 2：WatchSetupDialog 显示为白色空白区块

**现象**：点击「📁 文件夹监控」按钮，弹出一个白色空白矩形，无任何表单内容。

**原因链条**：
1. 原始代码 `new WatchSetupDialog(...)` 创建对话框后未调用 `dialog.setVisible(true)`
2. 同批次引入的 `BatchRenameDialog` 有相同 bug，已在 `681c6d9` 修复，但 `WatchSetupDialog` 被遗漏
3. 此外构造函数内的 `loadFromConfig()` 无 null 守卫，若 `createFormPanel()` 执行失败会导致 NPE

**解决**：
- `MainController.onOpenWatchSetup()` — 保存对话框引用并调用 `dialog.setVisible(true)`
- `WatchSetupDialog.loadFromConfig()` — 添加 `if (watchFolderField == null) return` 防御性检查
- 清理构造函数中残留的 RED 背景调试代码和 `System.err.println`

**教训**：
- `flatlaf-extras` 使用 `FlatSVGIcon` 时必须显式声明 `jsvg` 依赖，不能依赖 Maven 传递解析
- UI 构造函数创建后必须 `setVisible(true)`，对比 `SettingsDialog.show()` 的正确模式
- 同类组件（BatchRenameDialog / WatchSetupDialog）的 bug 修复要批量检查，避免遗漏

### 涉及文件
- `pom.xml` — 添加 `jsvg:1.2.0` 依赖
- `controller/MainController.java` — `dialog.setVisible(true)` 修复
- `view/WatchSetupDialog.java` — 清理调试代码 + null 守卫

### 提交
`fix(project,view): 修复 JSVG 缺失导致启动崩溃 + WatchSetupDialog 空白`
`3c33759`
- 3 个文件，+11 / -2 行
