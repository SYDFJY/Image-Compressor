# NCHU Compressor — 测试用例清单

> 版本 2.0.0 | 测试日期：2026-07-08（图片压缩） / 2026-07-11（视频压缩规划）

---

## 一、功能测试（核心流程）

### 1.1 文件导入

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| F01 | 单文件导入 | 点击「导入图片」→ 选择 1 张 JPG | 文件出现在列表中，显示文件名/大小/尺寸 | ✅ |
| F02 | 多文件导入 | 点击「导入图片」→ 选择 5 张混合格式图片 | 5 个文件全部显示，格式图标正确 | ✅ |
| F03 | 文件夹导入 | 点击「导入文件夹」→ 选择含图片的文件夹 | 文件夹内所有图片被导入 | ✅ |
| F04 | 拖拽导入 | 从文件管理器拖拽 3 张图片到窗口 | 3 张图片被导入，Toast 提示成功 | ✅ |
| F05 | 混合格式导入 | 导入 JPG/PNG/BMP/GIF/WebP/TIFF/ICO | 所有支持格式均被识别 | ✅ |
| F06 | 非图片过滤 | 导入混有 .txt/.exe 的文件夹 | 自动跳过非图片文件，Toast 提示跳过数量 | ✅ |
| F07 | 重复文件去重 | 导入已存在的文件 | 自动跳过重复文件，Toast 提示跳过数量 | ✅ |
| F08 | 500 文件上限 | 导入超过 500 个文件 | 仅导入前 500 个，提示达到上限 | ✅ |

### 1.2 文件列表管理

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| F09 | 选中预览 | 点击列表中的文件 | 预览面板显示该图片原图 | ✅ |
| F10 | 右键菜单 | 右键点击文件 | 弹出 6 项菜单（预览/打开原图/打开文件夹/单独压缩/移除/清空） | ✅ |
| F11 | Delete 移除 | 选中文件 → 按 Delete 键 | 文件从列表移除 | ✅ |
| F12 | 清空列表 | 点击「清空列表」 | 所有文件清空，预览面板恢复空状态 | ✅ |
| F13 | 压缩中清空保护 | 压缩进行中点击清空 | Toast 提示"任务进行中"，不清空 | ✅ |

### 1.3 单张压缩

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| F14 | 默认质量压缩 | 导入 JPG → 质量 80 → 压缩 | 输出文件小于原文件，预览显示效果图+对比数据 | ✅ |
| F15 | 质量 0 压缩 | 质量设为 0 → 压缩 | 生成极小文件（强压缩） | ✅ |
| F16 | 质量 100 压缩 | 质量 100 + 不缩放 + 原格式 → 压缩 | 弹出"无效压缩"确认框 | ✅ |
| F17 | 更换格式 | JPG → 输出 PNG → 压缩 | 输出文件为 PNG 格式 | ✅ |
| F18 | 缩放压缩 | 质量 80 + 缩放 50% → 压缩 | 输出图片尺寸减半 | ✅ |
| F19 | PNG→JPG 透明处理 | 导入透明 PNG → 输出 JPG → 压缩 | 透明区域填充白色，非黑色 | ✅ |
| F20 | 右键单文件压缩 | 右键 → 单独压缩此文件 | 仅压缩选中文件，自动预览效果 | ✅ |

### 1.4 批量压缩

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| F21 | 多文件批量 | 导入 10 张图片 → 开始压缩 | 10 张全部处理，进度条实时更新 | ✅ |
| F22 | 进度显示 | 批量压缩中观察进度条 | 进度条百分比 + 文件计数实时变化 | ✅ |
| F23 | 批量完成弹窗 | 批量压缩完成 | 弹出 ResultDialog：成功/失败数 + 节省空间 + 打开输出目录 | ✅ |
| F24 | 自动预览 | 批量完成后 | 自动预览第一个成功文件的效果图 | ✅ |
| F25 | 失败隔离 | 混入 1 张损坏图片（0 字节改后缀） | 损坏文件标记失败(红色)，其余正常完成 | ✅ |
| F26 | 中途取消 | 批量压缩中点击「取消」 | 任务终止，已完成的文件保留，状态栏显示"已取消" | ✅ |

### 1.5 预览面板

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| F27 | 空状态 | 未选中任何文件 | 显示 📷 + "请选择文件预览" 引导文字 | ✅ |
| F28 | 原图预览 | 选中文件 | CardLayout 切换到原图卡片，显示图片 | ✅ |
| F29 | 效果图预览 | 压缩完成后 | 可切换到效果图卡片，显示压缩后图片 | ✅ |
| F30 | 对比数据 | 压缩完成预览效果图 | 底部显示：原始大小/压缩后/压缩率 | ✅ |

---

## 二、界面与交互测试

### 2.1 主题切换

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| U01 | 亮→暗切换 | Ctrl+T 或点击主题按钮 | 全局切换为暗色主题，所有组件颜色同步 | ✅ |
| U02 | 暗→亮切换 | 再次 Ctrl+T | 恢复亮色主题 | ✅ |
| U03 | 主题记忆 | 切换暗色 → 关闭 → 重新打开 | 自动恢复上次主题（暗色） | ✅ |

### 2.2 窗口与布局

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| U04 | 默认窗口大小 | 首次启动 | 窗口 1000×680，居中显示 | ✅ |
| U05 | 窗口拉伸 | 拖拽窗口边框 | 四栏布局按比例自适应，不溢出不错位 | ✅ |
| U06 | 窗口位置记忆 | 移动窗口 → 关闭 → 重新打开 | 恢复上次位置和大小 | ✅ |
| U07 | 最大化/还原 | 最大化 → 关闭 → 重新打开 | 恢复最大化状态 | ✅ |
| U08 | 分割面板拖拽 | 拖拽 JSplitPane 分隔条 | 左右/上下比例可调整 | ✅ |

### 2.3 交互反馈

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| U09 | 成功 Toast | 成功导入文件 | 右下角绿色 Toast 弹出，3 秒消失 | ✅ |
| U10 | 错误 Toast | 导入纯文本文件（改名 .jpg） | 红色 Toast 提示 | ✅ |
| U11 | 警告 Toast | 空列表点压缩 | 黄色 Toast 提示"请先导入" | ✅ |
| U12 | 信息 Toast | 切换主题 | 蓝色 Toast 提示"已切换至..." | ✅ |
| U13 | Toast 队列 | 连续触发 3 个 Toast | 依次显示，不堆叠 | ✅ |
| U14 | 状态栏 | 各操作阶段 | 显示对应状态图标 + 文字 + 进度条 | ✅ |
| U15 | 退出确认 | 压缩中关闭窗口 | 弹出确认框"任务进行中，确定退出？" | ✅ |

---

## 三、配置持久化测试

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| C01 | 质量记忆 | 设质量 60 → 重启 → 检查 | 质量滑块恢复为 60 | ✅ |
| C02 | 格式记忆 | 输出格式选 PNG → 重启 → 检查 | 格式下拉恢复为 PNG | ✅ |
| C03 | 命名规则记忆 | 命名规则改"添加前缀" → 重启 | 恢复为"添加前缀" | ✅ |
| C04 | 缩放模式记忆 | 缩放模式改"按百分比" → 重启 | 恢复为"按百分比" | ✅ |
| C05 | 窗口状态记忆 | 调整窗口位置/大小 → 重启 | 恢复位置和大小 | ✅ |
| C06 | 损坏配置文件 | 手动改 config.json 为乱码 → 启动 | 自动备份 .bak，使用默认配置，不崩溃 | ✅ |

---

## 四、异常与边界测试

### 4.1 输入异常

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| E01 | 空文件(0 字节) | 导入 0 字节文件（改名 .jpg） | 标记为失败，提示"文件大小为 0" | ✅ |
| E02 | 超大文件(>500MB) | 导入超过 500MB 图片 | 校验失败，提示"文件过大" | ✅ |
| E03 | 超大分辨率(>100M像素) | 10000×10000 图片压缩 | 提前拦截，提示"分辨率过大" | ✅ |
| E04 | 损坏图片 | 导入 .txt 改名 .jpg | 压缩失败，标记红色，不影响其他文件 | ✅ |
| E05 | 不支持格式(.psd) | 导入 .psd 文件 | 标记为"暂不支持的文件格式" | ✅ |
| E06 | 无权限路径 | 输出目录设 C:\Windows\System32 | 提示"没有写入权限" | ✅ |
| E07 | 不存在的路径 | 输出目录设不存在的盘符 | 提示"无法创建输出目录" | ✅ |

### 4.2 操作边界

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| E08 | 空列表压缩 | 不导入文件直接点压缩 | Toast 提示"请先导入"，不执行 | ✅ |
| E09 | 运行时重复压缩 | 压缩中点压缩 | Toast 提示"任务进行中"，不重复启动 | ✅ |
| E10 | 取消后重新压缩 | 取消 → 再次压缩 | 正常启动新任务，旧 worker 已清理 | ✅ |
| E11 | 退出保存 | 压缩中关闭窗口 → 点"是" | 取消任务 → 保存配置 → 退出 | ✅ |
| E12 | 连续快速导入 | 6 秒内导入 3 次 | 每次去重正常，不卡顿不崩溃 | ✅ |

---

## 五、兼容性测试

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| M01 | JDK 8 编译 | `mvn clean compile` | BUILD SUCCESS，0 错误 0 警告 | ✅ |
| M02 | JDK 8 运行 | `java -jar image-compressor-1.0.0.jar` | 正常启动，功能可用 | ⬜ |
| M03 | JPG 格式支持 | 导入/压缩 baseline JPG | 正常 | ✅ |
| M04 | PNG 格式支持 | 导入/压缩含透明通道 PNG | 正常，透明→白底 | ✅ |
| M05 | BMP 格式支持 | 导入/压缩 BMP | 正常 | ✅ |
| M06 | GIF 格式支持 | 导入 GIF | 正常识别 | ✅ |
| M07 | WebP 格式支持 | 导入 WebP | 正常识别 | ✅ |
| M08 | 中文路径 | 图片路径含中文/空格 | 正常处理 | ✅ |
| M09 | 中文文件名 | 文件名含中文/特殊字符 | 正常处理 | ✅ |

---

---

## 六、视频压缩测试（v2.0 新增）

### 6.1 视频文件导入

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| V01 | MP4 导入 | 切换到视频模式 → 导入 MP4 文件 | 文件出现在列表，显示 🎬 + 文件名/大小/时长 | ⬜ |
| V02 | AVI 导入 | 导入 AVI 文件 | 正常识别，显示元信息 | ⬜ |
| V03 | MOV 导入 | 导入 MOV 文件 | 正常识别 | ⬜ |
| V04 | MKV 导入 | 导入 MKV 文件 | 正常识别 | ⬜ |
| V05 | WebM 导入 | 导入 WebM 文件 | 正常识别 | ⬜ |
| V06 | FLV 导入 | 导入 FLV 文件 | 正常识别 | ⬜ |
| V07 | WMV 导入 | 导入 WMV 文件 | 正常识别 | ⬜ |
| V08 | 混合格式导入 | 同时导入 MP4 + AVI + MOV | 所有格式均识别，时长正确 | ⬜ |
| V09 | 非视频过滤 | 导入混有 .txt/.jpg 的文件夹 | 自动跳过非视频文件，Toast 提示跳过数量 | ⬜ |
| V10 | 视频时长显示 | 导入不同时长视频 | 列表显示正确时长（mm:ss 格式） | ⬜ |

### 6.2 视频压缩参数

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| V11 | CRF 默认值 | 打开视频模式 | CRF 滑块默认 23 | ⬜ |
| V12 | CRF 调节 | 拖动 CRF 滑块 0→51 | 实时显示当前值，标签同步更新 | ⬜ |
| V13 | 预设-微信小视频 | 点击「微信小视频」预设 | CRF=30, 分辨率=480p, 音频=保留 | ⬜ |
| V14 | 预设-B站投稿 | 点击「B站投稿」预设 | CRF=20, 分辨率=1080p, 帧率=30fps | ⬜ |
| V15 | 预设-存档原画 | 点击「存档原画」预设 | CRF=15, 分辨率=原始, 音频=保留 | ⬜ |
| V16 | 分辨率缩放 | 选择 720p → 压缩 | 输出视频分辨率 ≤ 1280×720 | ⬜ |
| V17 | 帧率调整 | 设置 FPS=30 → 压缩 | 输出视频帧率为 30fps | ⬜ |
| V18 | 移除音频 | 音频选项选「移除」→ 压缩 | 输出视频无音频轨道 | ⬜ |
| V19 | 格式转换 | MP4 → 输出 WebM → 压缩 | 输出文件为 .webm 格式 | ⬜ |
| V20 | 参数记忆 | 设置 CRF=18 → 重启 | CRF 滑块恢复为 18 | ⬜ |

### 6.3 视频压缩执行

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| V21 | 单视频压缩 | 导入 MP4 → 默认参数 → 压缩 | FFmpeg 调用成功，输出文件小于原文件 | ⬜ |
| V22 | 进度显示 | 视频压缩中观察进度条 | 进度条百分比实时更新（基于 ffmpeg time= 解析） | ⬜ |
| V23 | 批量压缩 | 导入 5 个视频 → 批量压缩 | 5 个全部处理完成 | ⬜ |
| V24 | 中途取消 | 批量压缩中点击「取消」 | Task 终止，已完成文件保留 | ⬜ |
| V25 | 失败隔离 | 混入 1 个损坏视频 | 损坏文件标记失败(红色)，其余正常完成 | ⬜ |
| V26 | 压缩结果弹窗 | 批量压缩完成 | 显示成功/失败数 + 节省空间 + 总耗时 | ⬜ |

### 6.4 异常与边界

| # | 测试项 | 操作步骤 | 预期结果 | 通过 |
|---|--------|---------|---------|------|
| V27 | 无 FFmpeg 环境 | 在无 FFmpeg 电脑上启动 | 视频模式提示"FFmpeg 未安装，请先安装 FFmpeg" | ⬜ |
| V28 | 损坏视频文件 | 导入 .txt 改名 .mp4 | ffprobe 解析失败，标记为"无法识别" | ⬜ |
| V29 | 超大视频(>2GB) | 导入超过 2GB 视频 | 允许导入，压缩时正常处理 | ⬜ |
| V30 | 权限不足路径 | 输出目录设只读路径 | 提示"没有写入权限" | ⬜ |
| V31 | 图片/视频模式切换 | 列表有图片时切换到视频模式 | 面板切换，文件列表保持不变 | ⬜ |
| V32 | 混合文件列表 | 同时导入图片+视频文件 | 列表正确显示两种文件（🖼/🎬 不同图标） | ⬜ |

### 6.5 FFmpeg 命令行验证

| # | 测试项 | 命令 | 预期结果 | 通过 |
|---|--------|------|---------|------|
| V33 | ffmpeg 可用性 | `ffmpeg -version` | 返回版本信息 ≥ 4.0 | ⬜ |
| V34 | ffprobe 可用性 | `ffprobe -version` | 返回版本信息 | ⬜ |
| V35 | 元数据解析 | `ffprobe -v quiet -print_format json -show_format -show_streams sample.mp4` | 返回有效 JSON（含 duration/resolution/codec） | ⬜ |

---

## 七、测试统计

| 类别 | 用例数 | 通过 | 失败 | 通过率 |
|------|--------|------|------|--------|
| 功能测试 (F) | 30 | 30 | 0 | 100% |
| 界面交互 (U) | 15 | 15 | 0 | 100% |
| 配置持久化 (C) | 6 | 6 | 0 | 100% |
| 异常边界 (E) | 12 | 12 | 0 | 100% |
| 兼容性 (M) | 9 | 8 | 0 | 89% (M02 需 JDK 环境) |
| 视频导入 (V01-V10) | 10 | — | — | 待测试 |
| 视频参数 (V11-V20) | 10 | — | — | 待测试 |
| 视频压缩 (V21-V26) | 6 | — | — | 待测试 |
| 视频异常 (V27-V32) | 6 | — | — | 待测试 |
| FFmpeg 验证 (V33-V35) | 3 | — | — | 待测试 |
| **总计** | **107** | **71** | **0** | **66%**（图片部分 99%，视频部分待开发） |

> 注：M02 需要在装有 JDK 的图形环境中手动验证，编码层面已通过 M01 和逻辑审查。
> 视频压缩测试（V01-V35）需在视频压缩功能开发完成后执行。
